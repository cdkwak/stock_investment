# Archived Data Status Snapshot

> Historical snapshot captured before the 2026-08-27 agent-routing
> compaction. It is evidence only, not current authority. Relative links are
> preserved as historical text and may no longer resolve from this archive.

# Data Layer Status

This is the sole current Data routing authority. It is subordinate only to
`AGENTS.md`, the current user instruction, and
[Project Status](../project/PROJECT_STATUS.md). Archive, evidence, pilot,
queue, audit, and source-guide material never authorizes an operation.

## Start here

For a Data task, read only in this order:

1. this status;
2. [Dataset Index](DATASET_INDEX.md) for the selected dataset and path;
3. [Source Registry](SOURCE_REGISTRY.md) and, when a fallback is in question,
   [Source Fallback Policy](SOURCE_FALLBACK_POLICY.md);
4. only the contract, active-research item, state/checkpoint, or active runbook
   named below for that task.

Do not routinely read `research/`, `operations/`, or `docs/archive/`.

For sovereign yields, curve comparisons, equity linkage, or bond-ETF analysis,
preserve the distinctions in
[`SOVEREIGN_YIELD_BOND_ETF_SEMANTICS.md`](SOVEREIGN_YIELD_BOND_ETF_SEMANTICS.md);
the document is a semantic boundary, not promotion or predictive-use authority.

## Current boundary

| Field | Current value |
|---|---|
| Status date | 2026-08-27 KST |
| Project routing | `DATA` is the primary operational domain within domain-parallel engineering. Agents may use public endpoints and credentials already injected from project `.env` for market, macro, realtime, and read-only account data; implement contracts and collectors; retain Landing; validate/promote; and manage schedules without call-by-call approval. `.env` values and direct identifiers must never be disclosed. Orders, transfers, purchases, subscriptions, binding external agreements, and broker mutations remain forbidden. GUI, Features, offline Backtest/ML, portfolio simulation, and local paper simulation may proceed independently with non-overlapping scopes while the existing final holdout remains sealed. |
| Data state | `AUTONOMOUS_PUBLIC_AND_AUTHENTICATED_API_OPERATIONS_ACTIVE / BLOCKED_LAST_RESORT / PERMISSION_ONLY_GATES_SUPERSEDED / UNIFIED_YAHOO_30M_AUTOMATION_ACTIVE / BOK_TREASURY_1710_FINALITY_OBSERVATION_ACTIVE / SLOT_SPECIFIC_KR_DAILY_AUTOMATION_ACTIVE / CANONICAL_EQUITY_BOUNDED_CATCHUP_ACTIVE / CANONICAL_AND_BACKTEST_SEPARATE`. Existing route evidence and quality gates remain useful, but permission-only `NO_REPEAT`, retry-zero, one-shot, expired-window, manual-only, provider-activation, or fresh-user/Lead-approval requirements do not block new work under the standing runbook. Agents choose provider-appropriate rate limits, retries/backoff, pagination, windows, and schedules while preserving idempotency and valid prior data. A queue item becomes `Blocked` only when no safe in-scope work remains and a required secret/entitlement is unavailable, a protected-resource escalation was rejected, an excluded user-only financial/legal/access action is required, or an exact future provider publication/session/cooldown time has not arrived; a timed wait releases its lane. |
| Common foundation | `COMPLETE_WITH_EXPLICIT_DEFERRED_GATES / AUTOMATION_ROUND7_LIVE`: typed operations/auth metadata, fail-closed failure policy, registry-driven planning, run lock/CAS checkpoint, runtime coverage projection, and deterministic Health V2 are implemented. The current registries contain 37 operations entries and 80 universe rows; 30 datasets have `automation_enabled=True` and all 30 are Health-acceptable. The 2026-08-27 01:13 KST actual-user scheduled readback validates all 35 runtime datasets with zero failures. The former disabled `kr_treasury_yield_daily` failure was an exact-directory ACL omission; under explicit user authorization, `k4545` now has inheritable Modify on that one root and its 103 existing descendants while owner and prior ACEs remain unchanged. Scheduler success still projects runtime coverage from automation-enabled rows only, while universe Health exposes every managed and unmanaged probe. This includes the five canonical-equity dependency-chain datasets, the offline-gated KRX broad-index valuation route, the three exact-date KOSPI200 constituent/breadth outputs, and the seven KOSPI200 derivatives source/bridge/derived rows now validated through the eligible 2026-08-25 T+1 target. Semantic, source-rights, finality, predictive-use, and financial-mutation safety gates remain explicit; permission-only API-call gates are superseded by the standing authorization above. |
| Completed live operations | The unified Yahoo task's 2026-08-27 01:02 KST natural scheduled run returned process result 0 with `PASS`, 17/17 accepted routes, zero failures, and zero history writes: 13 routes use completed 30-minute bars while `^VIX/^FVX/^TNX/^TYX` retain provider-native 15-minute bars. The ten production `STOCK_DATA_*` definitions remain the release-readiness set; an eleventh evidence-only task, `STOCK_DATA_BOK_TREASURY_DAILY`, is separately installed and semantically read back for exact daily 17:10 execution, `StartWhenAvailable=false`, `IgnoreNew`, and `PT15M`. The three slot-specific KR definitions remain at exact 09:10/14:10/20:30 with matching `--scheduled-slot` and `PT30M`; only `_2030` has bounded `StartWhenAvailable=true` catch-up, while `_0910` and `_1410` retain missed-start replay disabled. Toss 30m permits missed-start invocation because its pre-credential calendar/current-window gate never backfills an earlier boundary and returns API zero outside the active window. Its Windows repetition duration is `PT6H`, yielding exact endpoint-inclusive wakes from 09:00 through 15:00 and excluding 15:30. Release readiness validates the exact latest due receipt even when a strictly newer pointer is a fully verified `INELIGIBLE/API0` catch-up. Recovery of the missed 2026-08-24 20:30 occurrence disclosed repeated invocations: immutable Landing shows liquidity at 17:38:45Z, liquidity plus credit at 17:41:38Z, and credit at 17:41:55Z. The overwrite-only last log cannot prove the intermediate process failure, so its cause remains unresolved. Both same-occurrence liquidity/credit `STABLE` claims were withdrawn to `PROVISIONAL`; all four Landing objects remain preserved. A durable scheduled-for/slot receipt is now exclusively created before provider-capable lanes, and exact replay returns `NOOP_OCCURRENCE_ALREADY_CLAIMED` with API calls 0. The next distinct 09:10 occurrence may perform the authorized confirmation. The old combined task and five legacy tasks remain absent. The latest retained provider-free release smoke has `release_blockers=[]`: production scheduler definitions 10/10, due outcomes 8/8, Health managed rows 30/30 acceptable, external calls 0, and data/scheduler mutations 0. `LastTaskResult=0` alone remains process evidence, never advancement proof. |
| Natural Yahoo repeat verification | The subsequent 2026-08-27 01:32 KST Windows occurrence also returned result 0 and `PASS`: 17/17 accepted, failures/retries/history writes all zero. Yahoo `^VIX` advanced to `15.61` for the completed 01:30 KST provider bar; this is a provider-native 15-minute, delayed/indicative display observation, not an official Cboe realtime claim. |
| Raw/Normalized/Canonical rule | Preserve layers. Promotion, source substitution, and PIT claims require the selected contract and passing evidence gates. Standing authority supplies ordinary execution permission; a missing contract/runbook is implementation work, not a reason to stop the whole task. |

### 2026-08-27 P0 refresh and scheduler reconciliation

- Manual retained-state reconciliation confirms KOSPI/KOSDAQ index and the
  published market-investor bridge through 2026-08-26, VKOSPI through
  2026-08-26, global index/ETF/futures through 2026-08-25, FRED VIXCLS through
  2026-08-25, FRED yields through their expected 2026-08-24 publication, and
  weekly H.10 FX through its expected 2026-08-21 observation. These dates are
  not silently compared as though they shared one publication calendar.
- `FRED_VIX_NEXT_BUSINESS_DAY_0840_CT` previously excluded the newest completed
  observation while the current XNYS session was still open. The availability
  resolver now evaluates every completed observation against its own next
  provider-business-day 08:40 CT boundary. The focused boundary test proves the
  transition from expected 2026-08-24 at 08:39:59 CT to 2026-08-25 at 08:40 CT.
  A live FRED run used one call to promote 2026-08-25; exact replay then used
  API zero and finished `SUCCESS`.
- Failure classification is now explicit: no target task is missing; the
  natural Yahoo 30-minute occurrence ran at 01:02 KST with process result 0 and
  moved all 17 current routes; no process interruption or missed occurrence is
  present; FRED yields/FX and KRX index valuation are provider-policy lags; the
  derivative T+1 Dashboard warning was a projection defect and is fixed; and
  the former Korean Treasury ACL probe failure is resolved and verified by an
  actual-user scheduled Health run with 35/35 probes valid.
- Scheduler Health projection is fail-closed for its managed scope: every
  automation-enabled row must be `VALIDATED`; a managed `NOT_PROBED`, unknown,
  or failed row rejects the occurrence. Disabled/manual failures remain visible
  in universe Health but cannot mask incomplete managed coverage.
- A second natural Yahoo occurrence at 01:32 KST again accepted all 17 routes,
  with zero failures, retries, or history writes. It moved Yahoo `^VIX` to the
  completed 01:30 KST bar (`15.61`) and the same Dashboard instance hot-read the
  new projection without restart.
- KRX KOSPI/KOSDAQ PER/PBR is correctly retained at 2026-08-25 until the
  `KRX_NEXT_TRADING_DAY_0910` boundary on 2026-08-27. The 09:10 natural
  occurrence, rather than a pre-publication synthetic or premature fetch, owns
  the 2026-08-26 advance and its subsequent Dashboard hot-read verification.

### 2026-08-22 수급·휴장일 데이터 품질 결정

- 국내 Dashboard `시장 수급`은 현재 보존된 KOSPI·KOSDAQ
  외국인/기관/개인 일별 순매수와 주간 누계를 사용한다. 마지막 완료 KRX
  거래일과 같은 날짜의 `DAILY_FINAL` 행은 `MARKET_CLOSED_LAST_FINAL`로
  읽을 수 있다. 이는 장마감 확정 일별값이며 30분/60분 장중 수급이 아니다.
  Toss·KB·LS 값을 평균하거나 조용히 병합하지 않는다.
- `exchange-calendars==4.13.2`가 포함하지 않은 공식 KRX 일회성 휴장일
  2026-06-03과 2026-07-17을 정책 버전 2의 명시적 override로 등록했다.
  근거는 KRX KIND 휴장 안내와 KRX 시장 휴장 원칙이다. 이 두 날짜는
  결측 보충 대상이 아니며 canonical 가격/수급 행을 만들지 않는다.
- 미국 시장에는 국내의 외국인/기관/개인과 같은 공짜 실시간 분류 수급을
  대응시키지 않는다. 공식 CFTC COT/TFF는 S&P 500·Nasdaq-100 선물의
  Dealer, Asset Manager, Leveraged Funds 등 **주간 포지션** 후보이며 실시간
  수급이 아니다. NYSE 실시간 피드는 별도 계약/권리 경로다. 따라서 미국
  수급 패널은 별도 Dataset Contract와 검증된 의미·데이터 권리 근거가
  갖춰질 때까지 숫자를 만들지 않는다. 조사와 비수치 프로토타입은 자율이다.

근거: [KRX 2026 휴장 안내](https://kind.krx.co.kr/external/2026/05/20/000110/20260520000197/32154.htm),
[KRX 휴장 원칙](https://global.krx.co.kr/contents/GLB/06/0602/0602010201/GLB0602010201T1.jsp),
[CFTC COT 공개 데이터](https://publicreporting.cftc.gov/stories/s/r4w3-av2u),
[CFTC TFF 분류](https://www.cftc.gov/dea/futures/deacmesf.htm),
[NYSE 실시간 데이터](https://www.nyse.com/market-data/real-time).

## Current operational routes

The standing autonomous API row authorizes public and existing-credential
market, macro, realtime, and read-only account operations by default. Lower rows
remain factual source evidence, but their permission-only `NO_REPEAT`,
retry-zero, one-shot, expired-window, `MANUAL_ONLY`, provider-activation, or
additional-approval language is
superseded. Their observed schema, semantic, provider, and failure facts remain
binding evidence until newer evidence replaces them.

Historical terminal labels and imperative phrases in a route row govern only
the exact consumed occurrence/idempotency key described by that row. They are
not authority to forbid a newly keyed date, range, provider-aware retry,
replacement route, research call, scheduler implementation, or independent
work. Unresolved meaning, rights, finality, or PIT evidence blocks only the
dependent promotion/claim/use; agents continue evidence collection and other
safe work and record the narrower gate. In those historical rows, `do not
retry/substitute` means do not replay the consumed occurrence; it is not a
provider-, symbol-, or endpoint-wide ban unless a current rights/access contract
independently establishes one.

When an older collector, operation file, or guard encodes a permission-only
one-shot/retry-zero/date-window restriction, agents may update that code and its
owning tests/runbook as normal implementation work. Preserve idempotency,
provider terms and rate limits, immutable Landing evidence, prior valid data,
and any restriction that protects unresolved identity, unit, finality, rights,
or PIT semantics. Do not preserve a historical approval gate merely because it
was once encoded in executable code.

| Route | Current action | Required detail |
|---|---|---|
| Autonomous public/authenticated API operations | `ACTIVE_STANDING_USER_AUTHORIZATION`. Agents may call public APIs and use existing `.env` credentials for market, macro, realtime, and read-only account data; discover, contract, collect, retain Landing, validate, promote, add health, and register/enable `STOCK_DATA_*` schedules without another permission for each normal step. Reasonable provider-aware retries, pagination, and new current windows are allowed. | [Active standing runbook](operations/AUTONOMOUS_PUBLIC_SOURCE_ONBOARDING.md). Never disclose `.env`, tokens, authorization material, or direct account identifiers. Orders, transfers, withdrawals, purchases, subscriptions, binding agreements, access-control bypass, and broker mutations remain excluded. |
| UR-111 core EOD recovery | `RESOLVED / SUPERSEDED_BY_CANONICAL_EQUITY_BOUNDED_CATCHUP / NO_ACL_GATE`. A 2026-08-26 current-identity read-only audit validates the exact KOSPI/KOSDAQ 2026 partitions for price, market cap, provider universe, canonical universe, and breadth plus the four owning price-cap, universe, accepted-date, and breadth state files. All ten partitions are contract-readable through 2026-08-25; no unverifiable aggregate path digest is used. Accepted-state SHA-256 `32d4bcdc5866f2a1bd977caecb07d75da016fc62b01e261c3c1145de076cfc3c` and breadth-state SHA-256 `3da9afd884e24f84220c7554d5406343f5dabbed362f975bf3a5747b3b02fc89` bind the same eight accepted/completed dates through 2026-08-25; breadth is `COMPLETE`, `pending_date=null`, and both transaction journals are absent. The earlier ACL blocker is closed without an ACL change. Future gaps advance only through the active slot-specific canonical operation, consecutively and without skipping, up to three eligible XKRX sessions, six logical API calls, or ten elapsed minutes per 14:10 or 20:30 occurrence. | [Active canonical scheduler operation](operations/CANONICAL_EQUITY_DAILY.md), accepted state `data/state/canonical_equity_accepted_dates.json`, and breadth state `data/state/canonical_equity_breadth_status.json` |
| Equity price/cap/universe | Availability-sentinel maintenance only | [Runbook](operations/DATA_GO_KR_EQUITY_AVAILABILITY_SENTINEL.md) |
| KRX KOSPI/KOSDAQ index valuation | `LIVE_VALIDATED_THROUGH_20260825 / API_ZERO_REPLAY / 09:10_AUTOMATION_ACTIVE / NON_PREDICTIVE`. The reviewed range operation used exactly two retry-zero business calls and immutable Landing, accepted the identical eight XKRX sessions for KOSPI `1001` and KOSDAQ `2001`, and atomically promoted both markets plus state from 2026-08-12 through 2026-08-25. Production readback has 13,134 unique hash-bound rows (6,567 per market) across 54 Parquet files; same-target operation and scheduler-lane replay both used API 0. Publication/revision finality remains unresolved, so all use is descriptive and non-predictive. | [Active runbook](operations/KRX_INDEX_FUNDAMENTAL_DAILY.md), contract `kr_index_fundamental_daily.py`, state `data/state/kr_index_fundamental_daily.json`, and lane receipt `artifacts/scheduler_logs/KR_INDEX_FUNDAMENTAL_DAILY_last.json` |
| PIT-safe sector research inputs | `RESEARCH_CONTRACT_COMPLETE / NUMERIC_CONSUMER_UNAVAILABLE`. The Data-owned feasibility boundary inventories Korea and U.S. taxonomy, security/universe, half-open historical membership, and all ten `sector-research/v1` roles separately. Only the narrow retained KRX KOSPI200 market price comparator is `SUPPORTED`; candidate price/fundamental families remain `RESEARCH_ONLY`. Historical taxonomy/membership plus sector comparator, sector breadth, sector flow, and structural-decline evidence remain `UNAVAILABLE`; market breadth, current classifications, ETFs, similar labels, and cross-taxonomy names are forbidden substitutes. | [PIT feasibility contract](research/active/SECTOR_TAXONOMY_MEMBERSHIP_PIT_FEASIBILITY.md) and [Backtest consumer contract](../backtest/SECTOR_RESEARCH_CONTRACT.md) |
| KOSPI forward PER / forward PBR | `BOTH_UNSUPPORTED / NUMERIC_USE_FORBIDDEN`. Official KRX weighted PER/PBR remains a separate descriptive series and is never relabelled forward. Primary LSEG, FactSet, and MSCI documentation confirms that licensed estimate/index products exist, but does not close exact KOSPI identity, horizon, denominator, KOSPI-specific aggregation/loss treatment, vintage/revision timestamps, schema, entitlement, retention, Dashboard/derived-display rights, and cost. Forward PER and forward PBR were decided independently; forward PBR additionally lacks an evidenced forecast-book-value definition. | [Source decision](sources/forward_valuation/KOSPI_FORWARD_PER_PBR_SOURCE_DECISION.md). Reopen only when one candidate closes every semantic, PIT, schema, rights and cost gate in writing; no API/sample-value collection or provider purchase was performed. |
| Forward EPS/BPS/ROE and earnings revisions | `P1 CONTRACT_DRAFT_COMPLETE / FNGUIDE_SEMANTIC_CANDIDATE / RIGHTS_AND_HISTORICAL_PIT_BLOCKED / NUMERIC_USE_FORBIDDEN`. Official FnGuide/FnSpace material positively identifies a daily paid 12M-forward family containing EPS, BPS, ROE and short-horizon revision fields, and documents the FY1/FY2 calendarization concept. Its published license does not permit this project to assume local database/application rights, and exact publication vintages, contributors, restatement causes, KOSPI historical membership/weights and aggregation remain unresolved. The future contract preserves source vintages, publication/retrieval/availability separately, forbids current-history backprojection and keeps every derived regime field null until matching rights and PIT evidence exist. | [Active PIT contract](research/active/KR_FORWARD_EARNINGS_PIT_CONTRACT.md). No subscription, purchase, API call, numeric retention or display was performed. |
| FinanceDataReader fallback review | `BOUNDED_PILOTS_COMPLETE / TWO_POLICY_FAMILIES_ACCEPTED`. UR-108's active `fred_vix_daily:VIXCLS` fallback remains unchanged. The user-authorized Korean regular-close boundary now allows only exact `000660`/`005930` display observations: retry-zero pykrx Primary, then one FDR/Naver attempt only for typed timeout/HTTP/rate-limit failure. Exact-date/identity mismatch and schema/price errors fail closed; prior-valid state is preserved atomically. The route remains `display_only=true`, `pit_safe=false`, `AS_RETRIEVED`, with no canonical/history/Backtest promotion, provider call, or scheduler activation performed by this change. | [Completed bounded runbook](operations/FINANCEDATAREADER_BOUNDED_FALLBACK_PILOTS.md), current evidence matrix (artifact 미보존), [fallback policy](SOURCE_FALLBACK_POLICY.md), and `orchestration/kr_equity_regular_close.py` |
| Current-observation foundation | `IMPLEMENTED / DISPLAY_ONLY / PROVIDER_ROUTES_STANDING_AUTHORIZED / PROMOTION_GATED`. UR-118 provides exact-identity atomic current-observation storage and API-zero replay; UR-131 adds durable pre-adapter claims, restart-safe 30/60-minute due controls, process locking and injected adapters; UR-132 provides a future-date manifest-gated FDR daily collector; UR-120/136 expose retained values and exact numeric-free reasons through local-only GUI reads. Agents may register and operate bounded public/existing-credential provider routes under the standing runbook. The Dashboard's current 30-minute timer rereads local state only; changing that implementation is allowed after contract and tests. UR-139 found no current-observation/display-Landing import or path read in the frozen feature/Backtest stack; this is structural isolation only and does not grant PIT eligibility. UR-138's ledger protects exact occurrences, not all new dates/windows. | [Current-observation supervisor runbook](operations/CURRENT_OBSERVATION_ACQUISITION_SUPERVISOR.md), [FDR future collector runbook](operations/FDR_FUTURE_DISPLAY_DAILY.md), next-eligible matrix (artifact 미보존), Backtest isolation audit (artifact 미보존), [GUI Status](../gui/GUI_STATUS.md), and [111-row source inventory](ALL_DATA_SOURCE_COVERAGE_20260821.csv) |
| Dashboard current-readiness local projector | `UR-235 TERMINAL_CONSUMED_API_ZERO / ONE_USE_COMPLETE`. UR-235 used one canonical CSV projection at aware clock `2026-08-21T21:34:00+09:00`, with terminal UR-193 SOXX and exact UR-232 retained-Landing global60m envelopes, explicit confirmation, durable preimage backup, atomic readback, and no-write replay. Visible: SOXX two rows plus global60m detail FX/futures rows; the official USD row remains numeric-free. The latter maps indicative FX only to detail; Treasury rows remain continuous-futures prices, never yields. Manifest/runbook are terminalized. | [UR-235 terminal runbook](operations/DASHBOARD_CURRENT_READINESS_LOCAL_PROJECTOR.md); runner `dashboard_current_readiness_projector.py` |
| UR-214 Naver mobile-home USD/KRW 19:00 window | `FAILED_BOUNDED_PRE_RESPONSE_TRANSPORT_20260821 / NUMERIC_DATA_NOT_ACCEPTED / NO_REPEAT`. The independent 19:00 ledger durably claimed and consumed its one GET (timeout 10; retry/redirect/fallback/Auth/cookie/environment zero), then stopped with sanitized `ConnectionError` before an HTTP body. No Landing, fresh FX_USDKRW validation, projection, or non-FX write exists; the prior shared projection is preserved. UR-211 remains an independent terminal 18:30 route. | [UR-214 exact runbook](operations/NAVER_HOME_UR214_USDKRW_1900.md) and execution evidence (artifact 미보존) |
| UR-221 Naver mobile-home USD/KRW 19:30 window | `FAILED_BOUNDED_PRE_RESPONSE_TRANSPORT_20260821 / NUMERIC_DATA_NOT_ACCEPTED / NO_REPEAT`. The independent 19:30 ledger durably claimed and consumed its one GET (timeout 10; retry/redirect/fallback/Auth/cookie/environment zero), then stopped with sanitized `ConnectionError` before an HTTP body. No Landing, validated FX_USDKRW, projection, or non-FX write exists; the prior shared projection is preserved. UR-211/UR-214 remain immutable terminal routes. | [UR-221 exact runbook](operations/NAVER_HOME_UR221_USDKRW_1930.md) and execution evidence (artifact 미보존) |
| UR-227 Naver mobile-home USD/KRW urllib 19:45 window | `FAILED_BOUNDED_PRE_RESPONSE_TRANSPORT_20260821 / NUMERIC_DATA_NOT_ACCEPTED / NO_REPEAT`. The independent `urllib.request` 19:45 ledger durably claimed and consumed its sole GET (fixed public User-Agent/Accept; timeout 10; redirects/retry/fallback/Auth/cookie/proxy-environment inspection zero), then stopped with sanitized `URLError` before an HTTP body. Landing and projection writes are zero; the prior shared projection is preserved and terminal replay is API 0. This route/window result is not provider-wide. | [UR-227 completed runbook](operations/NAVER_HOME_UR227_USDKRW_1945.md) and execution evidence (artifact 미보존) |
| UR-194 Nasdaq Composite official info pilot | `FAILED_BOUNDED_PRE_RESPONSE_TRANSPORT_20260821 / NUMERIC_DATA_NOT_ACCEPTED / NO_REPEAT`. The exact distinct official route `GET api.nasdaq.com/api/quote/COMP/info?assetclass=index` durably preclaimed and consumed its sole GET (timeout 10 seconds; retry/redirect/fallback/Auth/cookie/environment zero), then stopped with sanitized `ConnectionError` before an HTTP response. No body/headers/Landing, identity, index-points unit, ET provider timestamp, session/realtime proof, projection, GUI value, canonical/history, scheduler, or Backtest action exists. The durable checkpoint permits API-zero inspection only; do not retry or substitute the route, symbol, or endpoint. | [Completed route runbook](operations/NASDAQ_COMP_INFO_CURRENT_UR194.md) and sanitized evidence (artifact 미보존) |
| UR-195 Nasdaq Composite official summary pilot | `FAILED_BOUNDED_PRE_RESPONSE_TRANSPORT_20260821 / NUMERIC_DATA_NOT_ACCEPTED / NO_REPEAT`. The exact distinct official route `GET api.nasdaq.com/api/quote/COMP/summary?assetclass=index` durably preclaimed and consumed its sole GET (timeout 10 seconds; retry/redirect/fallback/Auth/cookie/environment zero), then stopped with sanitized `ConnectionError` before an HTTP response. UR-194's consumed `/info` route was not repeated. No body/headers/Landing, identity, index-points unit, ET provider timestamp, session/realtime proof, projection, GUI value, canonical/history, scheduler, or Backtest action exists. The durable checkpoint permits API-zero inspection only; do not retry or substitute the route, symbol, or endpoint. | [Completed route runbook](operations/NASDAQ_COMP_SUMMARY_CURRENT_UR195.md) and sanitized evidence (artifact 미보존) |
| UR-196 Stooq Nasdaq Composite candidate | `FAILED_BOUNDED_SEARCH_PRE_RESPONSE_TRANSPORT_20260821 / QUOTE_DISABLED / NO_REPEAT`. The exact Stooq identity-search document route `GET stooq.com/q/?s=nasdaq%20composite` durably preclaimed and consumed its sole search GET (timeout 10 seconds; retry/redirect/fallback/Auth/cookie/environment zero), then stopped with sanitized `ConnectionError` before a response. No Stooq symbol, index identity, intraday interval, timezone, session/delay, or local-personal display terms were bound; the conditional quote budget remained `0/0`. No quote, Landing, numeric, projection, GUI, canonical/history, scheduler, or Backtest action exists. Do not assume `^NDQ` or retry/substitute this exact search route. | [Completed route runbook](operations/STOOQ_NASDAQ_CURRENT_UR196.md) and sanitized evidence (artifact 미보존) |
| UR-197 Nasdaq-hosted S&P 500 info pilot | `FAILED_BOUNDED_PRE_RESPONSE_TRANSPORT_20260821 / NUMERIC_DATA_NOT_ACCEPTED / NO_REPEAT`. The exact distinct official-host route `GET api.nasdaq.com/api/quote/SPX/info?assetclass=index` durably preclaimed and consumed its sole GET (timeout 10 seconds; retry/redirect/fallback/Auth/cookie/environment zero), then stopped with sanitized `ConnectionError` before an HTTP response. No body/headers/Landing, SPX/S&P 500 identity, index-points unit, ET provider timestamp, session/realtime proof, projection, GUI value, canonical/history, scheduler, or Backtest action exists. The durable checkpoint permits API-zero inspection only; do not retry or substitute the route, symbol, or endpoint. | [Completed route runbook](operations/NASDAQ_SPX_INFO_CURRENT_UR197.md) and sanitized evidence (artifact 미보존) |
| UR-167 Naver mobile-home remaining windows | `COMPLETED_EXACT_WINDOWS_20260821 / NO_FURTHER_WINDOWS / DISPLAY_ONLY_PIT_BLOCKED`. The exact 14:30, 15:00 and 15:30 KST `NAVER_WEB:/` home HTML windows each consumed one durable GET (three total; timeout 10 seconds; retry/redirect/auth/cookie/fallback all zero) and retained a SHA-256-verified Landing response. At 14:30 and 15:00, KOSPI/KOSDAQ/USD-KRW passed strict identity/unit/realtime/today-KST<=60-minute gates. At 15:30 only USD/KRW passed (source 15:29); KOSPI/KOSDAQ lacked a visible price/timestamp and retained their valid 15:01 observations without overwrite. Gold/WTI remain numeric-free in every window: `VISIBLE_CONTRACT_UNIT_MISSING`. Atomic projection used no fallback; all replay calls are API zero. Original UR-166 capture/recovery is immutable. | [Completed terminal runbook](operations/NAVER_MOBILE_HOME_UR167_REMAINING_WINDOWS.md) and window evidence (artifact 미보존) |
| UR-176 Naver mobile-home post-close 16:00 | `COMPLETED_SINGLE_USE_NO_POST_CLOSE_NUMERIC_20260821 / NO_REPEAT`. The one 16:00 KST home GET completed with durable counts `1/1/1`, timeout 10 seconds and retry/redirect/fallback/auth/cookie zero; Landing SHA-256 is `a9da7464601961be4a412a097d0cc2d59128bf3f1a5b10dc9376c9f377f7702e`. No direct post-close numeric passed: KOSPI/KOSDAQ `VISIBLE_PRICE_OR_TIMESTAMP_MISSING`, USD/KRW `VISIBLE_STATUS_NOT_POST_CLOSE`, Gold/WTI `VISIBLE_CONTRACT_UNIT_MISSING`. Existing KOSPI/KOSDAQ 15:01 and USD/KRW 15:29 observations were preserved; replay API is zero. UR-167 remains terminal and untouched. | [Completed exact runbook](operations/NAVER_MOBILE_HOME_POST_CLOSE_1600.md) and sanitized evidence (artifact 미보존) |
| UR-211 Naver mobile-home USD/KRW 18:30 | `FAILED_BOUNDED_PRE_RESPONSE_TRANSPORT_20260821 / NUMERIC_DATA_NOT_ACCEPTED / NO_REPEAT`. The initial CLI preflight correctly received a Lead rework because an absent first-operation ledger had been mistaken for malformed state; the narrow fix now treats only absence as an empty initial ledger while malformed, unreadable, `ATTEMPTING`, and terminal states remain API-zero. After four focused CLI tests passed, the exact 18:30 half-open operation durably invoked its sole home GET and stopped with sanitized `ConnectionError` before an HTTP response (`raw_gets=1`, completed `0`, timeout 10 seconds, retry/redirect/fallback/Auth/cookie/environment zero). No Landing or USD/KRW projection was written; KOSPI/KOSDAQ/Gold/WTI were outside the projection allowlist and all prior observation bytes were preserved. This negative result is limited to the exact UR-211 window and does not generalize Naver or USD/KRW sources. | [Terminal runbook](operations/NAVER_HOME_UR211_USDKRW_1830.md) and sanitized evidence (artifact 미보존) |
| UR-191 Naver mobile-home next-session windows | `EXPIRED_UNEXECUTED_DATE_20260824 / PRE_DATE_PROVIDER_CALLS_ZERO / NO_RECLAIM`. The date-only 2026-08-24 manifest is no longer actionable. Its prior API-zero state remains evidence only; no later-date substitution, provider call, scheduler, canonical/history, Backtest, or GUI source change is authorized from this row. | [Expired date-specific runbook](operations/NAVER_MOBILE_HOME_UR191_NEXT_SESSION.md) |
| GUI current-display FDR/yfinance pilot | `FDR_000660_DISPLAY_UPDATED / YFINANCE_NUMERIC_NOT_ACCEPTED`. One new FDR 0.9.202 Naver-backed `000660` daily request returned four validated OHLCV+Change rows through 2026-08-20 with retry zero. Its successful body was retained and the exact latest close was promoted into a separate atomic current-display projection; the Korean equity screen labels it `UPDATED` with provider, source date, and retrieval timestamp without appending it to finalized daily history or Backtest. The distinct yfinance 1.6.0 `^GSPC` 5-minute history route stopped on `YFRateLimitError`; a separate one-connection/one-subscription WebSocket attempt received no message within 15 seconds. Both yfinance routes retain no numeric projection and must not be repeated. | [Completed bounded runbook](operations/GUI_FDR_YFINANCE_CURRENT_REFRESH.md) and evidence `artifacts/agent_runs/ur115/{gui_current_pilot_result,yfinance_websocket_result}.json` |
| Dashboard FDR daily current-display batch | `7 ROUTES VALIDATED / 3 TESTED ROUTES FAILED / DAILY_ONLY`. One ten-GET, retry-zero FDR/Yahoo batch retained validated 2026-08-20 observations for S&P 500, Nasdaq Composite, SOXX, NQ continuous, Gold, WTI, and VIX. Dashboard overlays the first six only when newer than retained non-prohibited data; the FDR/Yahoo VIX observation does not replace official FRED VIX. Exact KOSPI, KOSDAQ, and USD/KRW routes failed and are not repeated. All projections are display-only, `PIT_BLOCKED`, and excluded from finalized EOD, canonical, scheduler, automation, and Backtest. Overseas Naver price remains an unverified disabled candidate despite verified overseas listing support. | [UR-116 runbook](operations/DASHBOARD_FDR_TIERED_CURRENT_REFRESH.md), evidence `artifacts/agent_runs/ur116/fdr_dashboard_daily_result.json` |
| UR-129 FDR current-display operation | `PRIMARY_PRE_LANDING_CAUSE_NOT_RETAINED / NUMERIC_DATA_NOT_ACCEPTED / NO_REPEAT`. The exact FinanceDataReader 0.9.202 `NAVER:005930`, source-date-2026-08-21 operation consumed its one underlying Naver GET (timeout 10 seconds, retry zero) and failed closed before a successful body was available for Landing. The durable checkpoint retained the final typed no-alternate circuit `FDR_DISPLAY_NO_ALTERNATE_ROUTE`, but not the primary pre-Landing failure code; it is therefore not generalized to Naver, FinanceDataReader, or Korean daily pricing. No response body/header was retained, and the UR-118 display-only projection, finalized history, canonical, Backtest, GUI, and scheduler remain unchanged. Checkpoint replay read the consumed date with provider API `0`. Neither this date nor the user's earlier `005930 / 2026-08-01..10` evidence may be repeated or substituted. | [Completed exact operation](operations/FDR_CURRENT_DISPLAY_DAILY.md) and accepted split result (artifact 미보존) |
| UR-174 Naver desktop 005930 current HTML pilot | `FAILED_BOUNDED_SCHEMA_GATE / NUMERIC_DATA_NOT_ACCEPTED / NO_REPEAT`. The exact 2026-08-21 desktop `005930` HTML route consumed raw GET `1` with timeout 10 seconds and retry/redirect/fallback `0/0/0`. Its successful `195101`-byte body was retained Landing-first and hash/read back as `ffd2ba4a1fb3c7885490beb197d2b0ce77b14fe27f7ba832669097ff40a9e136`, but it lacked the strict same-body observation schema. No identity/venue/KRW-share/time/session field was inferred, no typed projection was created, and local replay used raw GET/API `0/0`. This is terminal for the exact route/date only. | [Completed exact runbook](operations/NAVER_DESKTOP_005930_CURRENT_HTML_PILOT.md) and sanitized evidence (artifact 미보존) |
| UR-134 yfinance NQ WebSocket pilot | `TESTED_NQ_ROUTE_TIMEOUT_NO_MESSAGE / NUMERIC_DATA_NOT_ACCEPTED / NO_REPEAT`. The exact unauthenticated yfinance 1.6.0 `NQ=F` scope consumed one connection and one subscription, waited 15 seconds, received zero decoded messages, and stopped with reconnect/retry/HTTP/support/Auth/cookie counts zero. It did not observe or infer `PricingData.time` units and produced no Landing/current observation, GUI value, history, canonical or Backtest mutation. This is limited to the tested symbol/access/time condition and is not a Yahoo/yfinance-wide unsupported result. | [Completed runbook](operations/YFINANCE_NQ_WEBSOCKET_PILOT.md), sanitized evidence (artifact 미보존), and closed result (artifact 미보존) |
| UR-135 LS t8412 current 15m pilot | `FAILED_BOUNDED_OAUTH_STAGE_20260821 / NUMERIC_DATA_NOT_ACCEPTED / NO_REPEAT`. The exact `005930` provider-date-2026-08-21 process consumed OAuth `1` and stopped before its t8412 business POST (business `0`, retry/continuation `0`). No successful Landing, typed observation, or projection exists; the checkpoint replayed with provider calls `0`. This is an exact OAuth-stage negative result only, not LS/t8412-wide unavailability. The completed 2026-08-12 calls and this consumed 2026-08-21 route must not be repeated or substituted. No canonical/history/Published/Backtest/GUI/scheduler/account/order action occurred. | [Completed bounded runbook](operations/LS_T8412_CURRENT_15M_PILOT.md) and closed result (artifact 미보존) |
| Toss KOSPI current-display pilot | `COMPLETED_ONE_SHOT_TYPED_OAUTH_OR_TRANSPORT_FAILURE_20260821 / NO_REPEAT`. UR-126's only allowed route was Toss `GET /api/v1/market-indicators/prices` with exactly `symbols=KOSPI`, expected KST provider date 2026-08-21, and fixed one OAuth + one serial market GET maximum. The attempt consumed OAuth=1 and stopped on sanitized `TossInvestHTTPError` before the market GET (market=0); Landing, current-observation projection, canonical/Published data, scheduler, and Backtest remain unchanged. The exact date is consumed and must not be retried or substituted with KOSDAQ/candles/investor/accounts/orders. | [Completed one-shot runbook](operations/TOSS_MARKET_CURRENT_OBSERVATION_PILOT.md); state `data/state/toss_market_current_observation_pilot.json` |
| UR-245 Toss domestic indices 30m | `EXPIRED_UNEXECUTED_DATE_20260824 / PRE_DATE_PROVIDER_CALLS_ZERO / KOSPI200_UNSUPPORTED / NO_RECLAIM`. The date-only 2026-08-24 manifest is no longer actionable. Its exact Toss adapter remains limited to KOSPI and KOSDAQ; no KOSPI200/KPI200 identity is verified. No later-date substitution, provider call, GUI/history/canonical/Backtest, or scheduler action is authorized from this row. | [Expired date-specific runbook](operations/TOSS_DOMESTIC_INDICES_UR245_30M.md); manifest `data/state/toss_domestic_indices_ur245_activation.json` |
| UR-246 Toss domestic current recurring 30m | `AUTOMATION_ACTIVE / EXACT_0900_TO_1530_WINDOW / OUTCOME_RECEIPT_STATE_MACHINE_VALIDATED / NATURAL_20260826_1300_TERMINAL_COMPLETE_4_OF_4 / CONCURRENT_POINTER_NONREWIND_VERIFIED / EXACT_RECEIPT_BINDING_VERIFIED / POWER_RESILIENT / KOSPI200_UNSUPPORTED`. The scheduler-safe coordinator independently composes 000660/005930 equities plus KOSPI/KOSDAQ indices behind an offline `ExchangeTradingCalendar(KR)` gate before any daily manifest, credential, or transport. The installed `STOCK_DATA_TOSS_DOMESTIC_30M` definition read back `Ready` with one weekday 09:00 trigger, 30-minute repetition for 6 hours, one supported wrapper action, `IgnoreNew`, a 25-minute execution limit, `StartWhenAvailable=true`, battery start allowed, battery-transition stop disabled, and `WakeToRun=true`; wakes end at 15:00 and the half-open 15:30 boundary is excluded. A delayed wake binds only the current floored half-hour, so closed/off-window recovery is API zero and earlier boundaries never backfill. The CLI binds and immutably claims one sanitized half-hour occurrence before runtime credential or transport construction, publishes a bounded terminal receipt with anonymous fixed-route outcomes and exact call counts, and advances a verified atomic last pointer without rewinding. A crash-released process-shared advisory lock serializes immutable-terminal validation, pointer validation, comparison, and atomic replacement. Deterministic cross-process newer/older coverage proves the older updater cannot publish after the newer pointer; equal-time conflicts, non-canonical receipt bindings, receipt-content mismatches, lock timeouts, and replacement failures all preserve the prior valid pointer. Exact terminal replay is provider API zero; malformed or incomplete receipt state fails closed. The first post-implementation natural 13:00 KST occurrence completed without manual trigger as `ELIGIBLE / TERMINAL_SUCCESS`: four anonymous slots `COMPLETE`, OAuth `1`, business calls `4`, exit `0`, reason `NONE`, and exact terminal/last-pointer time agreement. Provider-free readback and a three-file scan found no public route identity, URL, authentication/account key, exception marker, leftover staging entry, Landing-body read, or manual replay. The superseded `PT6H30M` definition produced one terminal `INELIGIBLE / API0` 15:30 receipt on 2026-08-26; it is preserved rather than rewritten. Release readiness validates the exact 15:00 due receipt independently and accepts a newer pointer only when that newer terminal is itself strictly valid and `INELIGIBLE`. Windows process result remains insufficient without the validated terminal and pointer agreement. | [Recurring runbook](operations/TOSS_DOMESTIC_UR246_RECURRING_30M.md); coordinator `collect_toss_domestic_ur246.py`; registration `register_toss_domestic_ur246_task.ps1` |
| UR-141 Toss stock current quote | `FAILED_BOUNDED_OAUTH_STAGE_20260821 / NO_REPEAT`. The endpoint-distinct Toss `GET /api/v1/prices?symbols=005930` route consumed one OAuth attempt and stopped before the business GET (business `0`, retry/fallback `0`). No successful body/Landing, current-observation projection, GUI value, history/canonical/Backtest, or scheduler change exists. Its durable attempt state records only the sanitized failure class and counts, so the exact endpoint/identity/date cannot be retried or substituted. This negative OAuth-stage result is limited to the exact Toss route and does not generalize to another provider or endpoint. | [Completed exact runbook](operations/TOSS_STOCK_CURRENT_QUOTE_UR141.md) |
| UR-239 Toss 000660 current quote | `TERMINAL_STALE_PROVIDER_TIMESTAMP_NO_REPEAT_20260821 / NUMERIC_FREE`. This distinct `GET /api/v1/prices?symbols=000660` route consumed exact OAuth `1` + business GET `1`, timeout10/retry/redirect/fallback zero, and retained/hash-read its successful Landing. Strict provider timestamp was 19:59:59 KST, over two hours old at the 22:12 KST operation, so today-KST/<=60m rejected it. No projection/numeric/current GUI/history/canonical/Backtest/scheduler action exists; prior state is preserved and exact route/identity/date is no-repeat. | [Terminal runbook](operations/TOSS_STOCK_CURRENT_QUOTE_UR239.md); state `data/state/toss_stock_current_quote_ur239.json` |
| UR-240 Toss 000660 retained NXT-close recovery | `COMPLETED_API0_RETAINED_RECOVERY / DISPLAY_ONLY_POST_CLOSE / NO_REPEAT`. UR-239's 498-byte successful Landing hash-read as `576019ac260bf2e6ce97f6683bb60fb5f1fb39beaaa1785abdd21d806bad78d5`; it proves `000660`, KRW, price and aware 19:59:59 KST timestamp. Under the current user's route-local exception, absent venue/session is recorded as `TOSS_NXT_CLOSE_INFERRED_FROM_EXCLUSIVE_TIME_WINDOW`, `venue_inferred=true`, never provider-declared NXT and never live. It atomically created/read back isolated display-only/PIT-blocked `toss_000660_nxt_session_close_ur240.json` (`KRW per share`, `POST_CLOSE_SNAPSHOT`); replay and all provider/OAuth/GET/retry/fallback actions are zero. GUI/history/canonical/Backtest/scheduler remain untouched. | [Completed recovery runbook](operations/TOSS_000660_NXT_CLOSE_UR240_RECOVERY.md) and `artifacts/agent_runs/ur240/` |
| UR-241 Toss 005930 NXT session-close | `RETAINED_API_ZERO_INFERRED_NXT_CLOSE_20260821 / VENUE_INFERRED / NOT_LIVE / DISPLAY_ONLY / PIT_BLOCKED`. The independent exact Toss route consumed OAuth `1` + business GET `1`, timeout10/retry/redirect/fallback zero, then terminally retained/hash-read its 497-byte Landing. Its single 005930 row has currency `KRW`, exact typed unit `KRW per share`, timestamp 19:59:59 KST, and no provider venue/session. The route-local authorized retained-only recovery labels this solely `TOSS_NXT_CLOSE_INFERRED_FROM_EXCLUSIVE_TIME_WINDOW`, `venue_inferred=true`, never provider-declared NXT and never live. It hash-gates the full retained Landing before atomically writing an isolated `PROVISIONAL` display-only/PIT-blocked observation, then API-zero replaying; no further provider/OAuth/GET action is allowed. It does not meet the shared <=60m current gate or touch GUI/history/canonical/Backtest/scheduler. | [Terminal runbook](operations/TOSS_STOCK_NXT_CLOSE_UR241.md); state `data/state/toss_stock_nxt_close_ur241.json`; projection `data/state/current_observations/toss_005930_nxt_close_ur241.json` |
| UR-244 Toss Korean-equity 30m date-bound collector | `EXPIRED_UNEXECUTED_DATE_20260824 / PROVIDER_CALLS_0 / NO_RECLAIM`. The manifest was limited to the verified 2026-08-24 XKRX date and exact `000660`/`005930` half-hour windows. That date is past, so it is historical API-zero evidence only: no later-date substitution, backfill, provider call, GUI/history/canonical/Backtest, or scheduler action is authorized from this row. Current recurring Toss behavior belongs to UR-246 and its own active contract. | [Expired date-specific runbook](operations/TOSS_EQUITY_UR244_30M_FUTURE.md); retained runner `collect_toss_equity_ur244_windows.py` |
| UR-143 LS t1101 current quote | `FAILED_BOUNDED_OAUTH_STAGE_20260821 / NO_REPEAT`. The new LS t1101 route consumed OAuth `1` and stopped on a sanitized `ConnectionError` before the t1101 business POST (business `0`, retry/continuation/fallback `0`). No successful body/Landing, time composition, typed projection, numeric display, or higher-layer action occurred. Its typed time-only composition contract remains locally validated but does not convert this failed route into a number. This exact t1101 identity/date is terminal no-repeat; it does not generalize to LS, t1301, t8412, or any future independently reviewed route. | [Completed exact runbook](operations/LS_T1101_CURRENT_QUOTE_UR143.md) |
| UR-146 KB current quote/orderbook | `DOCUMENTATION_AND_ENROLLMENT_GATE / PROVIDER_CALLS_0`. The official KB Securities portal confirms OpenAPI requires a KB account plus service application and advertises market-data capabilities, but its public material does not expose an exact quote/orderbook endpoint/TR, request body, response schema, currency/unit, or explicit provider source timestamp. Retained Rev1 KB code/contracts cover only IVSA0070 and explicitly prohibit converting its inquiry token into a provider timestamp. OAuth is not a diagnostic route, so no auth or business request was made. | UR-146 gate evidence (artifact 미보존) |
| UR-147 Naver domestic-index current pilot | `FAILED_BOUNDED_PRE_RESPONSE_TRANSPORT_20260821 / NUMERIC_DATA_NOT_ACCEPTED / NO_REPEAT`. The exact distinct combined Naver web route `GET polling.finance.naver.com/api/realtime/domestic/index/KOSPI,KOSDAQ,KPI200` durably reserved and invoked its only raw GET (timeout 10 seconds; retry/redirect/fallback/auth/cookie/environment zero), then stopped with sanitized `ConnectionError` before an HTTP response. No body/headers/Landing, explicit `dt`, session/state/scale validation, projection or GUI value exists. The terminal checkpoint replays with provider API `0`; do not replay the consumed route/date/three-code occurrence. This is limited to that occurrence and does not generalize to UR-145 mobile quote, FDR daily, Naver broadly, or another newly keyed route under standing authority. | [Completed exact runbook](operations/NAVER_DOMESTIC_INDEX_CURRENT_UR147.md) and sanitized result (artifact 미보존) |
| UR-148 Naver 005930 current polling pilot | `FAILED_BOUNDED_PRE_RESPONSE_TRANSPORT_20260821 / NUMERIC_DATA_NOT_ACCEPTED / NO_REPEAT`. The exact distinct Naver public-web `GET polling.finance.naver.com/api/realtime/domestic/stock/A005930` route durably reserved and invoked its sole GET, then stopped with sanitized `ConnectionError` before an HTTP response (timeout 10 seconds; retry/redirect/fallback/auth/cookie/environment zero). No body/header/Landing, explicit KOSPI-vs-NXT venue proof, source `dt`, price/unit validation, projection or GUI value exists. Terminal state replays provider API `0`; do not replay this consumed route/date/identity occurrence. The negative result does not generalize to FDR daily, UR-145 mobile quote, UR-147 index polling, Naver broadly, or another newly keyed route under standing authority. | [Completed exact runbook](operations/NAVER_005930_CURRENT_POLLING_UR148.md) and sanitized result (artifact 미보존) |
| UR-152 Naver mobile-basic 005930 current pilot | `FAILED_BOUNDED_PRE_RESPONSE_TRANSPORT_20260821 / NUMERIC_DATA_NOT_ACCEPTED / NO_REPEAT`. The exact distinct mobile-basic `GET m.stock.naver.com/api/stock/005930/basic` route durably reserved and invoked its sole GET, then stopped with sanitized `ConnectionError` before an HTTP response (completed `0`; timeout 10 seconds; retry/redirect/fallback/auth/cookie/environment zero). There is no body/header/Landing, explicit source time, mobile-schema validation, projection or GUI value. Terminal state returns provider API `0`; do not replay this consumed route/date/identity occurrence. It does not generalize to UR-145 `000660`, either polling route, Naver broadly, or another newly keyed provider operation under standing authority. | [Completed exact runbook](operations/NAVER_MOBILE_BASIC_005930_UR152.md) and sanitized result (artifact 미보존) |
| UR-153 Naver mobile-basic 000660 30-minute collector | `FIRST_LATER_WINDOW_FAILED_PRE_RESPONSE_20260821T133000_KST / NO_REPEAT`. UR-145's accepted 000660 mobile-basic route gained a durable 30-minute KST windowed entrypoint, process lock, orphan no-repeat handling and strict reused schema/unit/session/time validation. Its initial `13:00` window was excluded; only the first later `13:30` window invoked one GET and stopped with sanitized `ConnectionError` before a response (completed `0`; timeout 10 seconds; retry/redirect/fallback/auth/cookie/environment zero). No Landing/new observation exists; the previous valid UR-145 projection was preserved and its route circuit atomically opened `NAVER_TRANSPORT_ERROR`. Same-window replay is API `0`; do not replay that occurrence. A newly keyed future window may run under standing authority and may close the circuit only after full success. | [Completed first-window runbook](operations/NAVER_MOBILE_BASIC_000660_30M_UR153.md) and sanitized result (artifact 미보존) |
| UR-160 Naver mobile-basic 000660 exact 14:00 KST window | `FAILED_PRE_RESPONSE_EXACT_WINDOW_20260821T140000_KST / NO_REPEAT`. Only the accepted UR-153 collector ran the independent 14:00 window; 13:00 and 13:30 remained untouched. The one GET was durably claimed/reserved and invoked, then stopped with sanitized `ConnectionError` before response (completed `0`; timeout 10 seconds; retry/redirect/fallback/auth/cookie/environment zero). No Landing/new observation exists; the UR-145 prior remains and the route circuit stays open generation `2`. Same-window replay API is `0`; do not retry this exact window. | [Completed exact runbook](operations/NAVER_MOBILE_BASIC_000660_1400_UR160.md) and sanitized result (artifact 미보존) |
| UR-161 Naver mobile-basic 000660 remaining windows | `ALL_MANIFESTED_WINDOWS_FAILED_PRE_RESPONSE_20260821 / NO_REPEAT`. The secret-free historical manifest scoped only 14:30/15:00/15:30 KST; its GUI-readable boundary never overrode the durable collector ledger. Earlier 13:00/13:30/14:00 windows were untouched. Each of the three independent manifest windows invoked one exact 000660 GET then stopped with sanitized `ConnectionError` before response (each completed `0`; timeout 10 seconds; retry/redirect/fallback/auth/cookie/environment zero). No Landing/new value exists; the latest accepted UR-145 timestamp remains `2026-08-21T04:26:15+00:00`, circuit stays open generation `5`, and every same-window replay is API `0`. This historical UR-161 manifest supplies no further key; newly keyed work uses the standing route. | [Completed manifest runbook](operations/NAVER_MOBILE_BASIC_000660_REMAINING_WINDOWS_UR161.md), 14:30 (artifact 미보존), 15:00 (artifact 미보존), and 15:30 (artifact 미보존) evidence |
| OpenDART corporate-action incremental evidence | `RAW_EVIDENCE_RESEARCH_ACTIVE / STANDING_API_AUTHORIZATION / PROMOTION_GATED`. The retained ECOPRO BM list receipt is the API-zero baseline; its exact consumed occurrence remains immutable. Agents may choose a newly keyed issuer/date scope, use the existing credential securely, retain Landing/Raw evidence, and apply provider-aware bounded retry without separate user/Lead approval. Contract-only identity/filing observations keep effective identity, revision parent, intraday receipt time and canonical event status unresolved, so factor/adjusted/canonical/predictive use remains evidence-gated. UR-106 financial statements remain separate. | [Active Raw-evidence runbook](operations/OPENDART_CORPORATE_ACTION_INCREMENTAL_PILOT.md), contract `opendart_corporate_action_intake.py`, offline gate (artifact 미보존), and [canonical identity gate](research/active/CORPORATE_ACTION_CANONICAL_IDENTITY.md) |
| OpenDART financial-statement pilot | `RAW_RESEARCH_ACTIVE / STANDING_API_AUTHORIZATION / HIGHER_LAYERS_GATED`. UR-106 preserves one isolated-`opendartreader==0.3.3` exact-scope receipt: retained ECOPRO BM `corp_code=01160363`, 2021 annual `reprt_code=11011`, consolidated `fs_div=CFS`, and official `fnlttSinglAcnt.json`. That same completed scope stays API-zero replay, while agents may run newly keyed bounded Landing/Raw scopes with the existing credential and provider-aware retry without separate approval. Its injected-transport runner validates/normalizes initial capture time before fetch, atomically creates immutable same-volume Landing, hash-verifies readback, parses readback only, and emits a sanitized checkpoint. Every typed failure preserves valid output. Rows keep raw-string amounts; PIT, revision lineage, redistribution, higher-layer promotion, corporate-action inference, and predictive Backtest use remain gated until supported. | [Active Raw-research runbook](operations/OPENDART_FINANCIAL_STATEMENT_PILOT.md), contract and runner `opendart_financial_statement_pilot.py`, deterministic replay evidence (artifact 미보존) |
| Canonical equity daily incremental | `LIVE_VALIDATED_THROUGH_20260825 / BOUNDED_CATCHUP_ACTIVE / API_ZERO_REPLAY`. Reviewed occurrences advanced accepted sessions through 2026-08-25, retaining hash-bound Landing bodies and atomically promoting the four canonical datasets plus exact-recomputed breadth. All five dataset families pass their latest-year contract readers; accepted and breadth state agree on eight dates, breadth is complete with no pending date, and both transaction journals are absent. Health reports all five at `latest=2026-08-25 / EXPECTED_LAG / VALIDATED`, matching the D+1 publication target without pretending that a 2026-08-26 observation is already published. Valid-empty/partial/mismatch continues to preserve production and degrade the bundle. | [Scheduler operation](operations/CANONICAL_EQUITY_DAILY.md) and [atomic transaction gate](operations/CANONICAL_EQUITY_DAILY_INCREMENTAL.md) |
| Global Yahoo/FRED artifacts | `FRED_DAILY_AUTOMATION_ACTIVE / YAHOO_INDEX_AND_FUTURES_AUTOMATION_ACTIVE_WITH_LIMITS`. Global indices are current through 2026-08-18 and the exact SP500/NASDAQ_COMPOSITE/NASDAQ100 bundle runs at 06:20 KST. NQ=F/GC=F/CL=F retain 252 completed bars each through 2026-08-18 and are descriptive `PIT_BLOCKED`; live-forming or incomplete endpoint rows fail closed. Yahoo volume is not used as a trusted activity indicator because the latest adjacent values were identical across all three futures symbols. H.15, H.10, VIXCLS, Yahoo indices, and Yahoo futures use independent provider-publication policies. `STOCK_DATA_FRED_DAILY` runs at 06:00 KST and `STOCK_DATA_GLOBAL_FUTURES_DAILY` at 22:10 KST. | [Runbook](operations/GLOBAL_CURRENT_REFRESH.md) and [availability contract](operations/FRED_OBSERVATION_AVAILABILITY.md) |
| Global ETF / SOXX | `AUTOMATION_ACTIVE / SOXX_ONLY`. Production is 2025-08-18..2026-08-18 with 252 rows matching 252 XNYS sessions. `STOCK_DATA_GLOBAL_ETF_SOXX_DAILY` is enabled at 06:10 KST; actual and second triggers returned result 0 and the second used API 0. Dashboard reads this local dataset; predictive use remains blocked and no other ETF symbol is enabled. | Contract `global_etf.py` and [SOXX onboarding](operations/GLOBAL_ETF_SOXX_ONBOARDING.md) |
| Nasdaq SOXX current observation | `EXPIRED_UNEXECUTED_WINDOW_20260821_2200 / NO_RECLAIM / DISPLAY_ONLY / PIT_BLOCKED`. UR-193's 17:30 through 21:30 calls are terminal accepted/no-repeat, except 18:00 terminal `EXPIRED_API_ZERO_NO_BACKFILL`; separate UR-190 17:08 remains no-repeat. The retained 21:30 observation is `529.73 USD per share` at provider time 21:30 KST. The unattempted 22:00 key is expired and cannot authorize a later call or date substitution. No scheduler, canonical/history, or GUI layout mutation is authorized. | [Expired exact-window runbook](operations/NASDAQ_SOXX_CURRENT_UR193_WINDOWS.md), 21:30 evidence (artifact 미보존); ledger `data/state/nasdaq_soxx_ur193_windows.json` |
| Nasdaq VIX intraday route | `TERMINAL_OFFICIAL_HTML_HTTP302_NO_REPEAT / NUMERIC_FREE`. UR-200's one official Nasdaq VIX page discovery GET was durably preclaimed then returned HTTP 302; redirect budget was zero, so it stopped before Landing/body, asset, or quote/chart operations. No exact VIX identity-to-intraday endpoint/schema/unit/timezone/session binding was obtained. This does not generalize SOXX and does not affect Cboe, Yahoo, FDR, UR-185, or UR-187; all remain excluded from this route. | [Terminal runbook](operations/NASDAQ_VIX_CURRENT_UR200_DISCOVERY.md); ledger `data/state/nasdaq_vix_ur200_discovery.json` |
| Nasdaq VIX info current (UR-218) | `TERMINAL_PRE_RESPONSE_CONNECTION_ERROR_NO_REPEAT / NUMERIC_FREE`. The exact distinct public `VIX/info?assetclass=index` route durably reserved/invoked its sole timeout-10 GET, then stopped on sanitized `ConnectionError` before a response (`1/1/0` reserved/invoked/completed). Retry/redirect/fallback/Auth/cookie/environment were zero. No body/Landing, source timestamp, identity/unit/session evidence or projection exists; terminal replay is API zero. | [Terminal runbook](operations/NASDAQ_VIX_INFO_CURRENT_UR218.md); ledger `data/state/nasdaq_vix_info_ur218_pilot.json`; evidence (artifact 미보존) |
| Nasdaq TNX intraday route | `TERMINAL_OFFICIAL_HTML_HTTP302_NO_REPEAT / NUMERIC_FREE`. UR-201's sole direct Nasdaq TNX page GET was durably preclaimed and returned HTTP 302; redirect budget zero stopped body/Landing, referenced asset, and quote/chart access. It did not prove Cboe 10-Year Treasury Note Yield Index/TNX identity, yield-percent scale, schema, provider timestamp/session, or <=15m native series. Yahoo, Cboe, SOXX, COMP, SPX, VIX, ETF/futures/FRED substitution remain excluded. | [Terminal runbook](operations/NASDAQ_TNX_CURRENT_UR201_DISCOVERY.md); ledger `data/state/nasdaq_tnx_ur201_discovery.json` |
| UR-219 Nasdaq-hosted TNX info current pilot | `TERMINAL_ROUTE_SCOPED_NUMERIC_FREE / TNX_INFO_1_CONSUMED`. The independently keyed literal `api.nasdaq.com/api/quote/TNX/info?assetclass=index` consumed one durable GET (timeout 10, retry/redirect/fallback/Auth/cookie/environment zero). Its 142-byte Landing body SHA-256 `cbe508b6af79789851ca0053cc7e528bbb2e5a443696388c74b51577a052136f` is `data=null / rCode=400 Symbol not exists`, so it proves no TNX identity, yield-percent scale, provider timestamp, session/delay/realtime state, or numeric value. Retained API-zero review terminalized it; no display projection or substitute exists. UR-201 HTML remains immutable/no-repeat. | [Terminal runbook](operations/NASDAQ_TNX_INFO_CURRENT_UR219.md); ledger `data/state/nasdaq_tnx_info_ur219.json` |
| Nasdaq Composite / S&P 500 chart current | `TERMINAL_ROUTE_SCOPED_NUMERIC_FREE / COMP_1_SPX_1_CONSUMED`. UR-215 consumed one separately claimed retry-zero literal chart GET each and retained/hash-read both bodies. COMP binds index identity but gives only stale/date-only `Aug 20` plus time-only ET labels and no explicit unit/session-delay/current contract; SPX is `data=null / rCode=400 Symbol not exists`. Both terminalized with API-zero retained review; no projection, substitution, or route-wide Nasdaq conclusion is allowed. | [Terminal runbook](operations/NASDAQ_INDEX_CHART_UR215.md); ledger `data/state/nasdaq_index_chart_ur215.json` |
| KR/US 60m V2 | The regular-session equity audit remains non-automated. A separate exact `GLOBAL_CONTINUOUS` allowlist accepted only `KRW=X`, `ZT=F`, `ZN=F`, and `ZB=F`. Provider-null OHLC bars are omitted without filling; live-forming bars are excluded; all four calls must validate before atomic promotion. Treasury instruments are continuous-futures prices, never yields. `STOCK_DATA_GLOBAL_MARKET_60M` is removed legacy task evidence, not an installed lane; its retained failure log and UR-232 API-zero recovery remain historical diagnostics. Current display-only global collection belongs to unified `STOCK_DATA_YAHOO_MARKET_30M`, and neither route writes history or Backtest data. Predictive use remains `PIT_BLOCKED`. | Contract `market_60m.py`; `orchestration/global_market_60m.py`; [UR-232 completed recovery](operations/GLOBAL60M_UR232_RETAINED_LANDING_CURRENT_RECOVERY.md); evidence `artifacts/agent_runs/ur232/global60m_retained_landing_current_recovery_20260821.md`; scheduler log `STOCK_DATA_GLOBAL_MARKET_60M_last.json` |
| Yahoo native 15m | `UNIFIED_YAHOO_30M_AUTOMATION_ACTIVE / OLDER_AND_REVISION_PRIOR_PRESERVATION_IMPLEMENTED / NATURAL_OCCURRENCES_INSPECTED / NO_NATURAL_OLDER_CASE_OBSERVED / XNYS_LANE_FAILED_CLOSED / UR231_WINDOWS_EXPIRED_NO_RECLAIM`. `RQ-20260826T050718-2EAA` gives a deterministically verified older completed 30m/15m candidate the explicit `OLDER_*_BAR_PRIOR_VALUE_PRESERVED` outcome. `RQ-20260826T093519-39E1` now gives an exact-route, exact-provider-timestamp, different-value candidate the explicit `REVISED_*_BAR_PRIOR_VALUE_PRESERVED` outcome. Both paths leave the prior projection byte-identical, do not write comparison/session/history state, and count as preserved; same-timestamp/same-value remains no-op, newer bars still project, and missing, malformed, or mismatched prior/provider/schema/identity evidence fails closed. Provider-free operation, runner, release-readiness, and scheduler acceptance coverage passes. Natural occurrences inspected without manual triggering did not produce an older completed-bar case. The historical 2026-08-26 05:02 KST candidate started at 03:50 and completed at 04:05 KST, exactly equal to the retained provider timestamp but with a different value; 09:32 and 10:02 had the same exact conflict. Those pre-fix receipts preserved the retained projection but reported `PARTIAL_FAILURE`. A read-only API-zero replay of retained 09:32 `^TNX` evidence under the current classifier returned `REVISION_PRIOR_PRESERVED` for candidate/prior timestamp `2026-08-25T19:05:00Z`, different exact values, and unchanged projection SHA-256. Exact 2026-08-19 validation passed for `CBOE_VIX` with 1 retry-zero call and 26 rows, and for `YAHOO_TREASURY_QUOTE` with 3 retry-zero calls and 23 rows per identity; both promoted independently, passed retained read-back, and immediately replayed with API 0. The former CBOE-VIX and Treasury-quote daily tasks are removed legacy definitions. Their retained lane logs remain evidence, while current 15m VIX/Treasury display projections run inside `STOCK_DATA_YAHOO_MARKET_30M` at minute 02/32 with zero history writes. UR-231 made no call: its secret-free independent current-window manifests were date-specific to 2026-08-21 and are now expired. UR-234 installed/read back that exact manifest only; both lane ledgers remain unattempted and no Landing/projection exists, but those keys authorize no later call or date substitution. The 05:20 `XNYS_MARKET_INDEX` command exposed the 30-minute completion buffer and selected 2026-08-18; its three-call budget is not repeated, unintended Normalized/state were rolled back to the prior absent state, and Landing is retained. XNYS remains disabled until a later exact-date validation. Yahoo Treasury identities remain indicative quote indices, never official yields. The retained Yahoo `^VIX` lane is an XNYS-aligned provider subset, not Cboe's official 15-second spot-VIX service; official spot-VIX shortened sessions and Yahoo aggregation equivalence remain intentionally unavailable/fail-closed. | [Active lane runbook](operations/YAHOO_MARKET_15M.md); [Expired UR-231 window runbook](operations/YAHOO_NATIVE15M_CURRENT_UR231.md); [session evidence](sources/MARKET_SESSION_RULES.md); contract `market_15m.py`; lane logs under `artifacts/scheduler_logs/` |
| Yahoo per-symbol option volume P/C research | `UR-098_TESTED_ROUTE_RATE_LIMITED / NUMERIC_DATA_NOT_ACCEPTED / HISTORICAL_ATTEMPT_COMPLETE`. UR-094 remains the completed exact `SPY` query1 HTTP-401 result and UR-104 the completed yfinance/curl_cffi rate-limit evidence. UR-098 used one `QQQ` standard-`requests` query2 GET and stopped before the conditional nearest-expiry request; no payload was retained or accepted. This proves only that historical unauthenticated route outcome. Agents may investigate a new public or existing-credential route under standing authorization with secure secret handling and provider-aware retry/backoff. Numeric display, automation, or promotion still requires current identity, unit, timing/finality, retention/rights, and schema acceptance. | [Historical UR-098 runbook/checkpoint](operations/YFINANCE_OPTION_REQUESTS_FALLBACK_PILOT.md), [historical UR-094 route record](operations/YAHOO_SYMBOL_OPTION_PCR_PILOT.md) |
| U.S. option volume/OI put-call source | `UNSUPPORTED / ENTITLEMENT_AND_FINALITY_NOT_CLOSED / NUMERIC_USE_FORBIDDEN`. Official Cboe, OCC, ORATS and licensed Cboe product routes were compared without calling a provider or retaining a value. Public-page visibility does not grant automated Landing retention or Dashboard reuse. No candidate currently closes exact venue/product/root scope, put-over-call volume/OI semantics, publication/revision finality, stable schema/call budget and local/remote display rights together. The Cboe webpage's `TOTAL` venue boundary is itself unresolved; that page, OCC-cleared industry scope, ORATS per-underlying aggregates, Korean PCR and Yahoo attempts remain non-substitutable. | [Current source decision and reopen gate](sources/us_option_pcr/US_OPTION_PCR_SOURCE_DECISION.md); contract-only schemas `contracts/us_option_pcr.py`; [ORATS guide](sources/orats/README.md) |
| BOK Korean Treasury daily | `FINALITY_OBSERVATION_ACTIVE / DAILY_1710_SCHEDULER_INSTALLED / BATCH_1_OF_3 / TIME_WAIT_RELEASES_WRITER_LANE / PUBLICATION_FINALITY_UNKNOWN`. The first fixed-window batch on 2026-08-26 selected provider-native 2026-08-26 common to all six tenors, used six `StatisticSearch` calls plus one separately retained official UI marker call with retry zero, wrote immutable diagnostic Landing before response-ledger entries, and made no Normalized write. `STOCK_DATA_BOK_TREASURY_DAILY` is installed for exact 17:10 KST with late replay disabled; its supported runner checks the window and retained batch count before loading credentials, returns an API-zero typed no-op outside the window, and stops provider calls automatically at the three-batch review gate. Retained hash/readback, same-window API-0 replay, complete-Landing/state recovery, and interruption reconciliation pass. Its next-provider-day comparison is pending and requires no fresh approval; the writer lane remains released until the next exact occurrence. Expected latest remains null; promotion and numeric Dashboard use stay evidence-gated until the required batches establish finality. | [Active finality-observation runbook](operations/BOK_ECOS_TREASURY_DAILY.md); [current evidence](sources/bok_ecos/817Y002_PUBLICATION_FINALITY.md); runner `scripts/maintenance/run_bok_ecos_treasury_finality_observation.py`; policy `orchestration/bok_ecos_treasury_daily.py` |
| Universe health scheduler | Ten market/Health `STOCK_DATA_*` release-gate tasks remain installed and `Ready`, including the exact Korean 09:10/14:10/20:30 definitions. A separate `STOCK_DATA_TOSS_ACCOUNT_DAILY` read-only account task is installed and `Ready` at 07:00 KST with exact one-action/one-daily-trigger readback, `StartWhenAvailable`, `IgnoreNew`, `PT5M`, battery-start support, and wake-to-run; account success is governed by its own runbook/receipt and is not inferred from task process state. The market runner refreshes Health once after all due lanes, including degradation. Canonical equity is active in the existing 14:10/20:30 task actions and advances exact-date KOSPI200 breadth inside its own atomic transaction; no separate breadth scheduler lane is required. The 20:30 v2 bundle also contains the bounded derivatives-price lane. Health contract-validates the five canonical outputs, all three KOSPI200 scope outputs, and all seven derivatives outputs through latest-year/runtime probes. | `scripts/register_data_operations_tasks.ps1`; [Toss account operation](operations/TOSS_ACCOUNT_SNAPSHOT_READONLY.md); [canonical scheduler operation](operations/CANONICAL_EQUITY_DAILY.md); [KOSPI200 breadth operation](operations/KOSPI200_CONSTITUENT_BREADTH_INCREMENTAL.md); [derivatives operation](operations/KOSPI200_DERIVATIVES_DAILY_INCREMENTAL.md); scheduler runners; `artifacts/scheduler_logs/`; `artifacts/daily_health/` |
| Dividend observations | Offline append of independently retained complete snapshots only | [Runbook](operations/DIVIDEND_OBSERVATION_APPEND.md) |
| KB IVSA0070 snapshot | `RETAINED_LANDING_COMPARISON_RECOVERED_API0 / PARTIAL_NORMALIZED_QUARANTINED_IN_PLACE / PUBLICATION_GATED / ISOLATED_RESEARCH_ACTIVE`. The exact 2026-08-21 occurrence remains immutable at OAuth 1 + IVSA0070 1. Exact Landing/baseline hashes were gated and all seven slice comparisons were recovered locally with API 0; global symbols remain `DATE_UNRESOLVED` and liquidity is `LAGGED_SOURCE_DATE`. No slice has the independent provider timestamp required by the Dashboard current gate. Three accidentally touched breadth partitions stay `DO_NOT_USE_OR_PROMOTE` until an atomic rollback/repair contract is tested; do not bypass protected ACLs or mutate them ad hoc. Agents may implement that recovery contract and make newly keyed isolated Landing-only/read-only calls under standing authority, with prior valid production preserved. Scheduler/promotion/GUI/current/history/Backtest use remains gated by semantic acceptance and successful recovery, not a fresh permission decision. | [Incident runbook](operations/KBSEC_DAILY_MARKET_SNAPSHOT.md) and [semantic boundary](research/active/KBSEC_SNAPSHOT_CONTRACT.md) |
| LS t8462 Raw | `DAILY_RAW_COLLECTION_COMPLETE_20260814 / READY_WITH_FINALITY_GATE`. The first post-full-session operation completed six `K2I/MKI × F/C/P` products across `D=Day/N=Night/U=All`: 1 OAuth, 18 retry-zero data calls, 18 Raw, 18 provenance, 19 ledger events, secret scan PASS, and target date in all scopes. Immediate same-date replay returned `NOT_EXECUTED_ALREADY_ATTEMPTED` before OAuth/API. Raw/provenance/checkpoint/descriptive GUI use remain allowed; Normalized/Canonical and predictive/PIT promotion remain forbidden. | [Runbook](operations/LS_T8462_DAILY_RAW_COLLECTION.md) and [active semantics](research/active/LS_T8462_DERIVATIVES_SEMANTICS.md) |
| VKOSPI daily | `AUTOMATION_ACTIVE / PIT_LIMITED`. A bounded empirical post-close policy (completed XKRX session plus 18:30 KST) is current through 2026-08-19. Replay was API 0; the lane now runs inside `STOCK_DATA_KR_MARKET_DAILY_2030` at the 20:30 KST post-close trigger. This does not upgrade predictive finality. | [Runbook](operations/KRX_VKOSPI_DAILY_INCREMENTAL.md), contract `vkospi_daily.py`, and `data/state/kr_vkospi_daily.json` |
| Market investor daily | `AUTOMATION_ACTIVE / CURRENT_20260826`. `refresh_toss_market_investor_daily.py` captures one latest page for each of KOSPI and KOSDAQ, requires the intended exact date in both, stages and validates the normalized source plus published bridge, preserves the established live-root ACLs, and rolls back both outputs on promotion failure. The lane runs inside `STOCK_DATA_KR_MARKET_DAILY_2030` at 20:30 KST. The 2026-08-26 production receipt used three logical API calls and advanced both markets; Health and the Dashboard bridge now agree on `latest=expected=2026-08-26 / CURRENT / VALIDATED`. Same-date preflight replay remains API zero without loading credentials. Predictive use remains `PIT_BLOCKED`. | `orchestration/toss_market_investor_daily.py` and `data/state/toss_market_investor_daily.json` |
| KOSPI200 exact constituent breadth | `LIVE_VALIDATED_20260825 / API_ZERO_REPLAY / CANONICAL_TRANSACTION_ACTIVE`. One retry-zero `MDCSTAT00601:1028` business response was stored immutably before normalization and validated as 200 unique exact-date members. The transaction preserved 2026-08-12 history and atomically promoted 200 membership rows, 200 member-price rows, one checkpoint, and 2026-08-25 breadth `127 advancing / 66 declining / 7 unchanged`, with previous session 2026-08-24 and no missing prices. Immediate operation and replay both used API 0. The canonical-equity 14:10/20:30 transaction targets only `latest_accepted_date`, recomputes breadth atomically, and fails closed on any partial membership or price scope; a separate `KOSPI200_BREADTH_DAILY` bundle lane is not required. Health validates all three outputs at 2026-08-25; the GUI now selects the newest retained exact-date row rather than assuming the dataset has only one historical row. Exact membership is never backprojected, and dates without exact observations remain unavailable for predictive features. | [Active incremental operation](operations/KOSPI200_CONSTITUENT_BREADTH_INCREMENTAL.md); [retained 2026-08-12 proof](operations/KOSPI200_CONSTITUENT_BREADTH_DAILY.md) |
| KOSPI200 constituent intraday Raw pilot | `LIVE_VALIDATED_EXACT_RAW_20260812_API0_REPLAY / RAW_RESEARCH_ACTIVE / PIT_PROMOTION_GATED`. The exact LS t8412 native-15m transaction used one OAuth plus two serial retry-zero/continuation-zero data calls for retained same-date members `000660` and `005930`. Both immutable Landing responses validated together and atomically produced 52 Raw rows (26 per symbol), an exact-date checkpoint, and a successful readback with zero duplicate primary keys. Immediate checkpoint replay used OAuth/data calls 0. Provider time-label meaning and historical revision freeze remain unknown, so every row stays Raw/PIT-blocked. Agents may use newly keyed bounded symbol/date scopes for Landing/Raw evidence under standing authority; full-200 expansion, promotion, scheduling, and predictive use require a scale/finality/PIT contract and validation. No credential, token, authentication header/payload/response may be recorded. | [Completed exact Raw pilot](operations/KOSPI200_INTRADAY_PRICE_PILOT_REVIEW_REQUIRED.md) and secret-free evidence (artifact 미보존) |
| Short selling daily | `TRADING_CURRENT_THROUGH_20260820 / SLOT_SPECIFIC_AUTOMATION_ACTIVE / BALANCE_AND_INVESTOR_RESEARCH_ACTIVE`. KOSPI and KOSDAQ normalized partitions and the successful exact-date transaction state both contain 2026-08-20. The contract-validated nested market/year probe covers this dataset. Trading runs first inside `STOCK_DATA_KR_MARKET_DAILY_0910` and is idempotently reconsidered by `_1410` and `_2030`; next-XKRX-session T+1 eligibility keeps a Friday observation pending until Monday. Balance and Investor remain separate semantic/operation lanes and are not merged into trading, but agents may implement and validate them without fresh approval. | [Exact-date operation](operations/MARKET_DAILY_INCREMENTAL.md), `orchestration/market_daily_incremental.py`, and `orchestration/runtime_coverage.py` |
| Toss short watchlist | `LIVE_VALIDATED_20260819_API0_REPLAY / PROVIDER_SPECIFIC_KRX_ONLY / FINALITY_OBSERVATION_ACTIVE`. The historical fixed-symbol transaction used OAuth 1 plus exactly two calls and atomically retained one exact 2026-08-19 row for each of `005930`/`000660`; readback has two rows, no duplicate key, exact integer shares/KRW, and `KRX_ONLY_PROVIDER_EOD`. Both provider updates were 18:13..18:14 KST. Official overlap is `NON_EQUIVALENT_KRX_ONLY_VS_KRX_NXT_COMBINED`, never merged. This one date does not establish recurring finality; newly keyed observations and evidence work are allowed under standing authority, while scheduler/GUI promotion waits for a tested finality policy. | [Validated bounded runbook](operations/TOSS_SHORT_WATCHLIST_DAILY.md); evidence `artifacts/agent_runs/ur026_toss_short_watchlist_live_20260820T091000+0900.json` |
| Toss account snapshot | `ACTIVE_READONLY / DAILY_0700_SCHEDULER_INSTALLED / PRIOR_VALID_PRESERVED / NEXT_NATURAL_VALIDATION_20260827`. The 2026-08-25 23:56 KST Account-page refresh remains the last accepted `SUCCEEDED / MANUAL / tossinvest_open_api / getHoldings` snapshot. The supported daily CLI reads only the exact three named settings, requires an explicit in-memory selector, durably claims the KST date before client construction, permits at most OAuth 1 plus holdings 1 and KRW/USD buying-power 2, and replays any same-date claim at API zero. GUI/manual and scheduled calls share a process lock and concurrent commit coalescing; only the identifier-free atomic projection and bounded receipt are retained. The installed Windows task read back `Ready`, daily 07:00, one action/trigger, `StartWhenAvailable`, `IgnoreNew`, `PT5M`, and wake-to-run. The bounded 2026-08-26 CLI occurrence ended `TERMINAL_FAILURE / FAILED_PRESERVED_PRIOR` at OAuth 1 and account calls 0; it did not modify the valid 2026-08-25 snapshot and is no-repeat. The next evidence gate is the natural 2026-08-27 07:00 occurrence. Runtime credentials, tokens, direct account identifiers, raw responses, cross-currency totals, account discovery in the scheduled path, orders, corrections, cancellations, transfers, withdrawals, automated trading, KB credentials, and family-account authentication remain forbidden. | [Active read-only runbook](operations/TOSS_ACCOUNT_SNAPSHOT_READONLY.md) |
| KB SSQM2952 account snapshot | `ACTIVE_READONLY / AUTONOMOUS_REFRESH_IMPLEMENTATION_ALLOWED / LIVE_VALIDATED_20260826`. The Git-retained official workbook proves exact `POST /api/v1/ssqm2952`, App Key/Token authorization, and the sole request field `excg_mktpr_ccd`; the desktop fixes it to integrated-market code `A`. No account selector exists in the request, so the app uses only the account permission bound to the configured credentials and never discovers or persists an identifier. The strict client requires `resultCode=200` and `processCode=0011`; redirects are disabled and ambient proxy/`.netrc` credentials cannot alter the authority boundary. Startup remains API-zero and the current Account click runs OAuth-if-needed plus one business call off the GUI thread. The 2026-08-26 bounded validation completed `SUCCEEDED`; atomic readback verified the identifier-free schema/provider/operation boundary without emitting response values. Agents may add bounded periodic/off-thread or OS-scheduled read-only refresh after owning tests, coalescing, prior-valid rollback, and failure-preserving checks pass; no per-cycle approval is required. Raw response retention, direct identifiers, orders, transfers, withdrawals, and every broker mutation remain prohibited. | [Active bounded read-only runbook](operations/KBSEC_ACCOUNT_SNAPSHOT_READONLY.md) |
| 아빠 manual holdings current-price cache | `OFFLINE_SUPPLIER_INJECTED_API_ZERO / REFRESH_IMPLEMENTATION_ACTIVE`. The accepted dated acquisition basis has a separate strict v1 cache contract bound by its normalized basis SHA-256. Only an explicit verified section/ticker-to-exchange-qualified-symbol map may enter the supplier; missing or typed-failed rows stay numeric-free. Accepted KRW prices retain provider, exchange, unit, aware as-of/captured-at and finality labels, while market value, weights, unrealized P/L and return are derived without rewriting acquisition facts. Any malformed or rejected refresh preserves the prior valid cache atomically. Agents may add the verified map, provider refresh, and schedule under standing authority with owning tests; account authentication, direct identifiers, cross-currency totals without an FX contract, orders, and transfers remain excluded. | [Current operation and contract](operations/FAMILY_ACCOUNT_HOLDING_CURRENT_PRICES.md) |
| Stock lending daily | `AUTOMATION_CURRENT_20260820 / SLOT_SPECIFIC_AUTOMATION_ACTIVE`. Official metadata defines D+1 business day after 13:00 KST. The retained latest combined-KR result reports detail, market, and participant all at expected/latest 2026-08-20 with `NOOP_IDEMPOTENT` and provider API calls 0, closing the earlier partial state. The lane runs in `STOCK_DATA_KR_MARKET_DAILY_1410` and is idempotently reconsidered by `_2030`; both installed definitions were semantically read back on 2026-08-25. | [Exact-date operation](operations/MARKET_DAILY_INCREMENTAL.md) and `orchestration/market_daily_incremental.py` |
| Liquidity / credit daily | `HISTORICAL_TWO_PASS_STABLE / RECENT_FINALITY_OBSERVATION_ACTIVE`. Official data.go.kr metadata identifies the two KOFIA operations and says only `실시간`; it does not define a publication clock or revision freeze, so the lending D+1 rule is not inherited. The retained 2026-08-06 observations are `STABLE`. The slot-specific Korean bundle performs an observation-only `_2030` capture and `_0910` comparison for the same latest completed XKRX date, independently per dataset with one retry-zero call each. Equal values report `STABLE/OK`; changed values report `REVISED/DIFFERENT`, reset the anchor, and do not replace prior valid production. This controlled schedule gathers recent finality evidence but does not yet establish predictive or general publication finality. | [Two-pass operation](operations/MARKET_DAILY_INCREMENTAL.md), `orchestration/market_daily_incremental.py`, and `data/state/finality/kr_{market_liquidity,credit_balance}_daily.json` |
| Derivatives daily chain | `AUTOMATION_ACTIVE / CURRENT_20260825 / T_PLUS_1_EXPECTED_LAG / RETAINED_RECOVERY_API0_REPLAY`. The terminal 2026-08-20 ACL-capture attempt retained its two retry-zero Landing responses and rolled production back through 2026-08-19. The first API-zero recovery also rolled back on ACL restoration; after the exact three protected Bridge/Basis/PCR roots granted inheritable `Modify` to both `k4545` and `CodexSandboxOffline`, the SHA-256-bound single-use reviewed retry completed 2026-08-20 with API 0. Oldest-first catch-up then advanced 2026-08-21, 24, and 25 with six source calls total and no retry. Source futures/options now contain 11,382 / 2,722,082 rows; Bridge futures/options 38,643 / 3,812,160; Basis 6,544; persisted PCR 4,233; Wall 250. All seven outputs end at the latest eligible 2026-08-25 with zero key/partition violations, no journal/staging residue, runtime coverage `VALIDATED`, and an API-zero idempotent replay. The 20:30 v2 lane is active with maximum three sessions, six calls, and 600 seconds; it never requests a date before its successor XKRX session completes. Revision/predictive finality, price PCR, Active/Gamma Wall, Basis-unit interpretation, and the pre-2010 gap remain unresolved. | [Active bounded operation](operations/KOSPI200_DERIVATIVES_DAILY_INCREMENTAL.md), contract `derivatives_price_authority.py`, runner `orchestration/derivatives_daily_live.py`, checkpoint `data/state/derivatives_price_daily_live.json`, and retained 2026-08-20 attempt/recovery evidence |
| Dashboard core daily readiness | Research, contract, Landing, collector, validation, and scheduler implementation are active under standing authority. Only accepted-dataset promotion and numeric Dashboard exposure depend on the dataset's readiness/finality gates. | [Runbook and current readiness matrix](operations/DASHBOARD_CORE_DAILY_INCREMENTAL.md) |

Yahoo futures completed-grid null handling is `PROVIDER_FREE_VALIDATED /
PRIOR_VALID_PRESERVATION / LIVE_QUOTE_SUBSTITUTION_PROHIBITED`. For the six
contracted continuous-futures routes `ZT=F`, `ZN=F`, `ZB=F`, `NQ=F`, `GC=F`,
and `CL=F`, only exact 30-minute grid starts are completed-bar candidates. A
null, partial, or non-finite newest eligible grid OHLC row cannot silently fall
back to an older bar or use Yahoo's irregular `regularMarketTime` quote row. An
exact valid prior remains byte-identical and emits
`FAIL_COMPLETED_GRID_OHLC_UNAVAILABLE_PRIOR_VALUE_PRESERVED`; without an exact
prior the route emits a separate typed failure. Both make the occurrence
`PARTIAL_FAILURE` and scheduler readiness fails closed because byte
preservation is not freshness success. Newer fully numeric grid bars still
advance. Six-route selection, prior-present/absent aggregation, forged-PASS
rejection, and release-readiness coverage pass with live calls and
history/canonical/Backtest writes zero. See [the active Yahoo lane
runbook](operations/YAHOO_MARKET_15M.md).

The KRX index daily route is implemented in
`src/stock_data/orchestration/kr_index_daily_live.py` and
`kr_index_daily_incremental.py`. It captures one explicitly finalized date in
three paced, retry-zero pykrx calls, persists immutable source and normalized
Landing before validation, then promotes both datasets under one lane journal,
checkpoint, and rollback unit. The lane now runs inside
`STOCK_DATA_KR_MARKET_DAILY_2030` at the 20:30 KST post-close trigger. The exact
lane state is `SUCCEEDED / finalized_market_date=2026-08-25 / api_calls=3` and
binds both `kr_index_daily` and `kr_kospi200_index_daily` as promoted with
`retained_latest=2026-08-25`; their per-dataset states report 21,398 and 9,453
rows respectively. Provider-free replay remains API zero. This operational
coverage update does not change either dataset's existing PIT/finality limits.

| Global current-only unified 30m | `NATURAL_SCHEDULE_PASS_20260827_0102 / SEVENTEEN_ROUTE_SESSION_AWARE / HISTORY_AND_BACKTEST_WRITES_ZERO`. The active allowlist has 13 completed-30-minute global routes (`^KS11`, `^KQ11`, `KRW=X`, `ZT=F`, `ZN=F`, `ZB=F`, `^GSPC`, `^IXIC`, `NQ=F`, `SOXX`, `GC=F`, `CL=F`, `BTC-USD`) plus four provider-native 15-minute quote routes (`^VIX`, `^FVX`, `^TNX`, `^TYX`). The natural 2026-08-27 01:02 KST scheduled occurrence accepted 17/17 with process result 0, zero failures, zero retries, two exact-prior preservations for closed KRX routes, and zero history writes. Native 15-minute bars are neither resampled nor relabelled as 30-minute bars; `^FVX/^TNX/^TYX` are Yahoo Treasury quote indices, not official Treasury yields. KOSPI/KOSDAQ values are finalized KRX closes and Yahoo is trace-only. Nasdaq/S&P 500/SOXX final completed bars remain bounded to the completed U.S. cash session. NQ/Gold/WTI are Yahoo vendor-continuous futures; Bitcoin is 24-hour. The installed task is `STOCK_DATA_YAHOO_MARKET_30M` at minute 02/32 (`PT30M`); the former 60m task is absent. The separate retained `market_price_15m_observation` Normalized segment covers 2026-08-19..2026-08-21 and is now correctly classified `STATIC_COMPLETE / NO_REFRESH`. | [Active runbooks](operations/GLOBAL_MARKET_CURRENT_60M.md) and [Yahoo native-grid runbook](operations/YAHOO_MARKET_15M.md); current receipt `artifacts/scheduler_logs/STOCK_DATA_YAHOO_MARKET_30M_last.json` |

Dashboard session presentation for these routes is calendar-derived. Korea is
reported as separate `KRX 09:00~15:30` and `NXT 08:00~20:00` states; NXT time is
only a session-display boundary and does not relabel Yahoo KRX index data as NXT.
U.S. cash regular and after-hours ranges are converted from America/New_York to
KST using the XNYS calendar, including DST. Card mini-graphs use the exact
comparison artifact's `previous_session_close` as their dashed baseline.
Nasdaq-100 futures and Bitcoin are visually reset at the current XNYS open;
the underlying Landing/session trace, canonical history, and Backtest inputs are
not truncated or rewritten.

USD/KRW is a separate indicative `KRW=X` current-only observation. Its card and
mini-graph use the exact prior provider session comparison and an 08:00 KST
visual reset. It does not replace the official daily H.10 series. Official daily
Treasury yields and FX rows may expose their latest finalized retained value and
date in the lower panel, but no missing provider timestamp is invented and no
daily row is called realtime.

## Full Dataset Universe reconciliation

The offline reconciliation now combines the 67 registered Dataset Contracts,
actual `raw/normalized/derived/published` roots, bounded retained Landing
evidence named by the Dataset Index, and the 37-row typed operations registry.
It yields 80 logical datasets, 52 economic-variable families, and 74 unique
typed physical-artifact scopes. Landing calls, partitions, and provider files
are not counted as datasets, and the read-only Option Wall contract is not
misreported as a persisted root. The detailed matrix is
[`artifacts/data_inventory/full_dataset_universe_multiaxis_20260818.csv`](../../artifacts/data_inventory/full_dataset_universe_multiaxis_20260818.csv),
and its typed source is
`src/stock_data/orchestration/dataset_universe.py`.

Before the original reconciliation, 33 rows were in the executable health
registry. Three accepted KOSPI200 exact-constituent/breadth contracts were later
added and are now exact-date automated operations, followed by one descriptive
KRX broad-index valuation route. Twenty-four
historical/research/provider-specific records were intentionally outside it,
and 19 contracted or retained operational records were not represented. The
typed universe registry now contains all 80, so current `REGISTRY_MISSING=0`;
the operations registry contains 37 rows; the KRX broad-index valuation route
and exact-date KOSPI200 scope are the post-reconciliation additions promoted to
bounded automation.
This separation makes an intentionally known dataset explicit
without making a Research-only, static, snapshot, or blocked dataset runnable.

All 80 rows now have independently validated display, research, and predictive
consumer decisions plus bounded reason codes. Display derives only from the
reviewed GUI-use contract; research derives only from a Dataset Contract and/or
retained evidence; predictive derives only from PIT evidence and remains
blocked for `PIT_LIMITED`, `PIT_BLOCKED`, `NON_PREDICTIVE`, and
`RESEARCH_ONLY`. Counts are display `23 ELIGIBLE / 17 LIMITED / 40 BLOCKED`,
research `61 ELIGIBLE / 19 LIMITED / 0 BLOCKED`, and predictive
`9 ELIGIBLE / 0 LIMITED / 71 BLOCKED`. Collection readiness, scheduler state,
freshness, and a retained file do not cross-promote any of these decisions.

The universe no longer uses the mixed `primary_classification` value as an
operational source of truth. That field remains only as a deprecated migration
projection. Independent totals reconcile to 80 on every axis: roles are 39
source, 8 source-observation, 12 Raw-observation, 6 derived, 5 published bridge,
7 snapshot, and 3 historical segment; grain is 62 daily, 4 weekly, 5 event, 7
snapshot, and 2 intraday. Derived and published rows declare dependency refreshes rather than
separate provider calls.

Operational readiness is 19 `READY_WITH_FINALITY_GATE`, 10 `READY_WITH_LIMITS`,
20 `MANUAL_ONLY`, 2 `IMPLEMENTATION_READY`, 8 `BLOCKED`, and 14
`NOT_APPLICABLE`. These are retained registry labels for dataset execution,
promotion, and scheduler readiness, not authorization to stop agent work.
`MANUAL_ONLY` and `BLOCKED` rows remain open for research, contracts, collectors,
tests, Landing/Raw evidence, and dependency fixes under the standing authority;
only the unsupported output/promotion remains closed. Intentional
provider-specific duplicates and retained completed historical segments are
`NOT_APPLICABLE`. No row is operationally
blocked merely because predictive PIT use is blocked.

Scheduler management currently has 30 datasets with `automation_enabled=True`: three
direct FRED inputs, three stock-lending datasets, short-selling trading, VKOSPI,
both KR index datasets, the market-investor bridge, the exact three-symbol
global-index bundle, symbol-bound SOXX, the three-symbol Yahoo futures dataset,
the canonical equity price/cap/universe/published/breadth chain, the
descriptive KRX broad-index valuation dataset, and exact-date KOSPI200
membership/member-price/breadth, plus seven KOSPI200 derivatives
source/bridge/derived outputs. All seven derivatives rows are now
Health-acceptable at `latest=expected=2026-08-25 / EXPECTED_LAG / VALIDATED`.
The delayed global 60m and native 15m observation datasets are retained
historical/current-display evidence and are no longer automation-enabled; the
active current display is the separate unified 30-minute polling operation. The four weekly CFTC records are static-complete
historical Raw observations, so they correctly remain no-refresh rather than a
weekly execution lane. Six blocked derived/bridge rows still retain
`UPSTREAM_DEPENDENCY` and `DEPENDENCY_DRIVEN` as their eventual refresh and
automation policies. Their scheduler stays fail-closed until dependencies pass,
while agents may fix those dependencies and implement/test the lane.

The Data Status GUI now expands the retained Health V2 facts through the typed
registry into an 80-row compatibility view and exposes
`Operational`, `Daily`, `Blocked`, `Research/Static`, and `All` filters. Its ten
compact columns are Dataset, Role, Cadence, Latest, Expected, Freshness,
Operational, PIT, Automation, and Source. It performs no provider call, data
mutation, or Landing scan. Health reconciliation validates the small Dashboard
artifacts and only the newest year partition of each larger lending dataset.
The Health V2 projector now serializes all six explicit
consumer fields (`*_consumer_eligibility` and `*_consumer_reason`) for every
row; this is metadata-only and does not alter GUI runtime behavior.

## Core operations reconciliation

The 2026-08-18 core report remains retained at
`artifacts/daily_health/core_data_20260818.json` as historical input. Its 33
operation rows are now accepted only as a known subset of the current 37-row
registry; the reconciliation records the three later KOSPI200 exact-date rows as
missing and still rejects any unknown ID. The artifact records freshness,
finality, operational eligibility and predictive eligibility independently, so
an unknown finality does not erase an exact retained-date comparison or make a
lane ready. The regenerated typed 80-row Health V2 projection uses `CURRENT`,
`EXPECTED_LAG`, `STALE`, `UNKNOWN`, and `NOT_APPLICABLE`; all 62 daily rows
resolve to XKRX, XNYS,
provider business/publication, event, or explicit no-market-calendar policy, so
`calendar unavailable daily rows=0`. Its generation/reference timestamps are
explicit, all 35 Dashboard/automation runtime probes validate, and actionable
incident count is 0 after excluding manual, unsupported, not-applicable,
permission and predictive-only gates. The current artifact has eight `CURRENT`,
23 `EXPECTED_LAG`, eight `STALE`, 14 `UNKNOWN`, and 27 `NOT_APPLICABLE` rows.
Its 30 managed rows are exactly eight `CURRENT` and 22 acceptable
`EXPECTED_LAG`, with zero managed stale/unknown rows.
Contract-valid runtime dates are
authoritative even when they regress; a failed probe becomes `UNKNOWN` instead
of falling back to an older apparently current value. Exact current readback
now validates the newest price/cap/universe/canonical/breadth partitions and
confirms their shared retained latest date as 2026-08-25. The breadth status is
`COMPLETE` through the same date with no pending date. Health reports all five
rows as provider-window-aware `EXPECTED_LAG / VALIDATED` at latest/available
target 2026-08-25 rather than inventing a same-day 2026-08-26 publication.
The three KOSPI200 scope outputs are likewise `EXPECTED_LAG / VALIDATED` at
2026-08-25. All seven derivatives outputs now use the explicit
`KRX_COMPLETED_SUCCESSOR_SESSION` policy and are also
`latest=expected=2026-08-25 / EXPECTED_LAG / VALIDATED`; Health no longer asks
for the ineligible same-session 2026-08-26 observation.

The scheduler lanes are classified individually. FRED, canonical equity,
exact-date KOSPI200 breadth, lending, VKOSPI, KRX index, market investor,
global index, global ETF/SOXX, global futures, and derivatives price
are scheduler-eligible and installed. Canonical equity completed its first
reviewed live advancement and now advances consecutive oldest eligible sessions
within explicit per-occurrence limits of three sessions, six logical provider
calls, and ten elapsed minutes; it stops at the first unresolved date and never
skips a session. LS t8462 remains `READY_WITH_FINALITY_GATE`; derivatives
investor and broker snapshot remain `MANUAL_ONLY`, while liquidity/credit keeps
its bounded observation-only schedule. The derivatives-price lane is current
through 2026-08-25 and active in the exact 20:30 v2 bundle. It advances
oldest-first within three-session/six-call/600-second bounds and stops at the
first unresolved date.

Canonical equity promotion now keeps durable SHA-256-verified backups and a
replacement journal for startup rollback. Accepted-date idempotency fails if an
expected target is missing. Canonical acceptance atomically records breadth as
pending, and both breadth partitions plus their completion checkpoint commit as
one transaction. The first live transaction advanced every output and state
from 2026-08-18 to 2026-08-19; subsequent bounded occurrences advanced the
same atomic family through 2026-08-25. Landing manifests bind to accepted state,
and exact breadth recomputation matches retained readback. Current-user readback
succeeds, so the earlier file-ACL blocker is no longer an active
scheduler gate. No ACL was changed. The runner still relies on transactional
readback and fails closed on any access regression.

The exact read-only reconciliation receipt is reproducible without relying on
filesystem enumeration order. Sort the following repository-relative POSIX
paths ordinally, serialize each as `path<TAB>lowercase_sha256<LF>` in UTF-8
without BOM, including the final LF, and SHA-256 the resulting bytes. The 14
current entries and aggregate are:

```text
data/derived/kr_market_breadth_daily/market=KOSDAQ/year=2026/data.parquet	6f68dd57f9c5ad800bef5574700193dff9385c08a8befb220c6c81d906912dec
data/derived/kr_market_breadth_daily/market=KOSPI/year=2026/data.parquet	4202d21e9620e2015f18c8416353f328e7d9da94dd14912f39d677be4660cbbf
data/normalized/kr_equity_market_cap_daily/market=KOSDAQ/year=2026/data.parquet	001b9a06cce2b746ad4b5c8ceb73264a1b4458a921a4635e0fea141bd2314f43
data/normalized/kr_equity_market_cap_daily/market=KOSPI/year=2026/data.parquet	5f78fefac39cc469b3566303be8e80c84911bc7e6882bbbe47cdf7d2d35a8eba
data/normalized/kr_equity_price_daily/market=KOSDAQ/year=2026/data.parquet	1a05fdcd8c8ed6f734744551a085ba19a11df5cb0cd00f9b1b8db37feccd9e23
data/normalized/kr_equity_price_daily/market=KOSPI/year=2026/data.parquet	b9b99bc74a10cb89a47fe67dae36da27c05643cd4fc3a5c08eb5c5129bf14663
data/normalized/kr_equity_universe_daily/market=KOSDAQ/year=2026/data.parquet	9400e9a7bdb2e1059c07a1d6c0ec338ff98efc0bc5dde0c71fb37de6e4d48d47
data/normalized/kr_equity_universe_daily/market=KOSPI/year=2026/data.parquet	4457a5d3c7012a944abc40f7a71c199b1e00eb4d91c7f11969e98bf486346897
data/published/kr_equity_canonical_universe_daily/market=KOSDAQ/year=2026/data.parquet	fbc81b8aaf580326cc269835cdaed8d2bd6503152a7118ff0a65c3799db4520e
data/published/kr_equity_canonical_universe_daily/market=KOSPI/year=2026/data.parquet	f413bca090b71ea9aaba920c57c33c4445e1b93fa60ffa6687f92815a936a8dc
data/state/canonical_equity_accepted_dates.json	32d4bcdc5866f2a1bd977caecb07d75da016fc62b01e261c3c1145de076cfc3c
data/state/canonical_equity_breadth_status.json	3da9afd884e24f84220c7554d5406343f5dabbed362f975bf3a5747b3b02fc89
data/state/kr_equity_price_cap_daily.json	e24938f2d7b3b6cd8c88028f108988a485816296bb16b39b9b9866bba7fb645d
data/state/kr_equity_universe_daily.json	f0e50fdd8431ab4df11a93d1e6567a3da525d08f4e5e3e1d247ff92dfdc08c84
```

Aggregate SHA-256:
`ddc7632c9412bc3848b4f4eb2ff245e8487ed8bd97d99ae2badf202a54a14c3a`.

## Active research

These are real unresolved semantic/rights/PIT questions. They block only the
claims or promotion that depend on the missing evidence. Standing Data authority
still permits research calls, Landing capture, contract work, and independent
operations that do not pretend the question is resolved.

| Priority | Unresolved question | Current state / next safe action |
|---:|---|---|
| 1 | Corporate-action canonical identity, revision links, availability, and complete adjustment terms | `CANONICAL_IDENTITY_BLOCKED`; retain source observations only. Bonus/free-issue, capital reduction and rights issue remain factor-blocked. Rights schedule rows cannot replace a final versioned event with exact class, entitlement, final subscription price, rights/exercise/fraction treatment and ex-right rule. Merger and company division are independent `IDENTITY_DISCONTINUITY_EVIDENCE_INCOMPLETE / CONTINUOUS_PRICE_CHAIN_FORBIDDEN` families; names, current ticker and `corp_code` never bridge histories. [Research](research/active/CORPORATE_ACTION_CANONICAL_IDENTITY.md) |
| 2 | KOSPI200 futures/options pre-2010 bulk-use terms | Official screen coverage is confirmed; research and terms verification are active, while bulk retention/use remains rights-gated until provider terms support it. [Research](research/active/KRX_PRE2010_DERIVATIVES_TERMS.md) |
| 3 | LS t8428 source boundary, calendar, publication, and revision semantics | `SOURCE_ERROR_BOUNDARY_STOP`; preserve Raw and the exact failed cursor receipt. Agents may investigate a new cursor/window or corrected implementation with provider-aware bounds. [Research](research/active/LS_SOURCE_BOUNDARIES.md) |
| 4 | LS t8462 prospective finality/revision and PIT operation | `LS_T8462_SEMANTIC_CLOSURE=CLOSED_WITH_LIMITS`: contracts, signed 100-million-KRW net purchase, and `D=정규/N=야간/U=전체` are empirically confirmed across all retained `K2I/MKI × F/C/P` families. The accepted 2026-08-14 post-full-session Raw capture and no-call replay now exist. Historical Option `U` keeps `AGGREGATE_FIELD_SEMANTICS`; observed history begins at a `SOURCE_HISTORY_BOUNDARY` of 2025-07-18. Historical and daily Raw remain `RESEARCH_ONLY_NON_PREDICTIVE`; official finality/revision timing remains unresolved and persisted Normalized/Canonical is forbidden. [Feature policy](config/LS_T8462_ANALYSIS_FEATURES.md); [Research](research/active/LS_T8462_DERIVATIVES_SEMANTICS.md) |
| 5 | ETF publication/revision timing and contract/PIT policy | Date-specific Universe/OHLCV Raw is complete; no current-list backprojection or promotion. [Research](research/active/KRX_ETF_PIT.md) |
| 6 | U.S. security master, historical OHLCV primary source, and SEC filing/fundamental PIT | Research, source comparison, contract design, and bounded Landing/Raw prototypes are active. Record the entitlement/source decision before promotion or broad automation. [Research index](research/active/README.md) |
| 7 | CFTC historical release timestamp | Native report dates are retained; `release_date=null` and predictive use stays blocked. [Research](research/active/CFTC_RELEASE_DATE_PIT.md) |
| 8 | KB IVSA0070 per-slice source/market date | Current snapshots remain provider-specific and provisional. [Research](research/active/KBSEC_SNAPSHOT_CONTRACT.md) |
| 9 | SOX official history access, licensing, EOD finality, and vintage policy | `RESEARCH_ACTIVE / RETENTION_AND_PROMOTION_RIGHTS_GATED`; SOX and the SOXX ETF are never substitutes. Agents may continue rights/source research and compliant prototypes; SOXX's own production route does not resolve SOX access or equivalence. [Research](research/active/SOX_SOURCE_RESEARCH.md) |
| 10 | Free historical valuation, consensus, revision, credit-spread, and SOX candidates | Official discovery is complete; agents may run bounded, terms-compliant research backfills and build contracts under standing authority. Production promotion still requires accepted meaning, rights, and PIT evidence. [Discovery](research/active/HISTORICAL_FREE_SOURCE_DISCOVERY.md) |

## Closed acquisition and semantic conclusions

These rows preserve current conclusions and forensic evidence. Do not overwrite
or replay an identical consumed idempotency key merely to obtain a different
answer. New dates/ranges, revised implementations, and evidence-driven retries
may proceed under standing authorization while preserving the old receipt.

| Area | Current conclusion | Remaining restriction |
|---|---|---|
| pykrx high-value Raw phase | `PYKRX_RAW_ACQUISITION_PHASE_COMPLETE`. Foreign ownership: 6,558/6,558 dates and 13,910,258 rows; equity fundamentals: 4,589/4,589 and 9,925,137 rows; ETF Universe/OHLCV shared source: 4,590/4,590 and 1,700,421 rows. | Agents may establish the missing contracts and PIT/publication/revision policy. Normalized promotion remains closed until those gates pass. |
| CFTC Legacy, TFF, and Disaggregated | Historical Landing Raw backfills are complete. | Report-family boundaries remain; `release_date=null`; predictive use blocked. |
| LS t1633 | `LIVE_SOURCE_HTTP_500_FAIL_CLOSED / NEW_DATE_RECOVERY_ACTIVE`. LS keeps the documented `/stock/program` t1633 operation active, while no official revision-freeze time is published. The policy accepts only the latest retained XKRX session strictly before the operation date. The historical 2026-08-19 run completed OAuth and retained the first KOSPI amount response as HTTP 200/one exact row; the second KOSPI quantity call returned HTTP 500. Redacted failure provenance and a failed transaction journal are retained; no Normalized root or completion checkpoint exists. | [Active bounded runbook](operations/LS_T1633_PROGRAM_TRADING_DAILY.md). Do not replay the same consumed occurrence; a future newly eligible date or corrected newly keyed attempt may run under standing authority with bounded retry and no fresh approval. Unit evidence remains `CONFIRMED_EMPIRICAL_MULTI_DATE`; predictive revision safety remains blocked. |
| Short-selling serving semantics | `SHORT_SELLING_SERVING_SEMANTICS=CLOSED_WITH_LIMITS`. `kr_short_selling_trading_daily` is the Dashboard official Primary. Its date regime is `KRX_ONLY` through 2025-03-03 and `KRX_NXT_COMBINED` from 2025-03-04. Toss short-selling and retained LS t1716 are separate per-symbol `KRX_ONLY_EMPIRICALLY_CONFIRMED` provider-EOD views, never an official aggregate fallback. | No verified intraday source. Same-date volume remainder is exact; amount remainder is exact only for an `EXACT_KRW` provider amount (Toss). LS `gm_value` is million-KRW truncated, so its amount remainder is `APPROXIMATE_FROM_TRUNCATED_PROVIDER_AMOUNT` and is not displayed as a numeric inferred amount. The exact-date official trading wrapper is offline atomic-ready; agents may perform bounded live validation, then enable its scheduler if the acceptance gates pass. |
| KOSPI200 spot index and Option Wall | Separate pykrx ticker `1028` daily production contains 9,453 rows through 2026-08-25. The active bounded derivatives chain now contains Source futures/options 11,382 / 2,722,082 rows, Bridge futures/options 38,643 / 3,812,160, Basis 6,544, combined PCR 4,233 (modern segment 1,626), and a recent-250 Wall ending on 2026-08-25. All seven chain rows are `EXPECTED_LAG / VALIDATED` at the completed-successor T+1 target and the 20:30 scheduler is active. | `PIT_SAFE_EOD_T_PLUS_1` applies only to the explicit same-date Wall/index join; never use the same close before its successor session completes. Maximum-OI strike is descriptive and is not an Active/Gamma Wall. Basis units remain source-native and unverified. Price PCR, provider revision/predictive finality, and the pre-2010 gap remain unavailable. Backtest continues to use its separate frozen root. |
| VIX / VKOSPI serving | The series remain separate. FRED `VIXCLS` covers 1990-01-02..2026-08-14 with 9,554 rows. Official KRX `MDCSTAT01201` code `1300` VKOSPI covers 2003-01-02..2026-08-14 with 5,830 Raw and Normalized rows; the 2026-08-12..14 pilot matched 3/3. | Both may serve `LATEST_FINAL_DAILY / PIT_LIMITED` with their own source and market date. FRED predictive use remains vintage-blocked; VKOSPI publication/revision timing remains unresolved. Early VKOSPI source rows may have close while OHLC/change fields are empty, preserved as null only in Normalized. |
| FINRA and Cboe | FINRA pilot/documentation evidence is retained with unresolved schema/rights gates; Cboe retained Landing is license-gated. | Research, terms verification, and compliant prototypes remain active. Automatic collection, fallback, or promotion requires accepted rights/schema evidence. |

## Evidence-gated outputs and deferred gaps

The classifications below limit the named output or use. They do not put an
agent or whole queue task into `Blocked` while research, contracts, tests,
alternative sources, or unrelated safe work remain.

| Class | Scope | Reopen condition |
|---|---|---|
| `RIGHTS_GATED` | Broad KRX derivatives investor sell/buy/volume history | Terms research and bounded non-retaining diagnostics may continue; broad retention/use requires explicit KRX rights evidence and a concrete use case. |
| `PIT_BLOCKED` | Index constituents and sector classification history | Official effective/change-date evidence; no daily brute force. |
| `DEFERRED_GAP` | ETF PDF, broader current-symbol fan-outs, and duplicate Toss routes | A bounded use case with a dated universe and a distinct economic variable. |
| `PREDICTIVE_USE_BLOCKED` | Raw candidates without publication/revision/availability policy | A reviewed contract and direct timing evidence. |

## Lifecycle rule

1. Start a genuinely unresolved investigation in `research/active/`.
2. Once a conclusion is accepted, record its current effect in this file,
   the Dataset Index, and/or Source Registry.
3. Move the detailed audit, pilot, backfill, or source capture note to
   `docs/archive/data/evidence/`; move superseded proposals and handoffs to
   `docs/archive/data/`.
4. Never use either location as active authorization. Keep
   `REVIEW_REQUIRED` material in active research until its stated review is
   actually resolved.
