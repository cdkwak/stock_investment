# 연구 계산 계층 구조 재구축 결과 (2026-09-06)

이 문서는 이번 엔진 재구축의 결과 파일이다. 새 백테스트 숫자나 새 기본값을 만들지 않았고, 전체 그리드·retained 결과 산출물·기존 `RESULTS_*` 문서는 실행하거나 갱신하지 않았다.

## 구조 변경

- `LadderSpec`의 다섯 필드 `drawdown_threshold`, `disp60_threshold`, `product_share_at_max`, `levels`, `base_exposure`는 하나라도 값이 없으면 그 필드 이름을 붙인 규칙 ⑥ 오류를 낸다. `levels`는 1..4 정수, `base_exposure`는 유한한 0..3 값으로 검증한다. 부분 가드는 가드가 없는 것보다 더 나쁘다 — 가드는 전부 아니면 전무이며, 한 인자를 고칠 때 같은 호출의 모든 인자를 본다.
- 최고 단계의 상품 비중은 `product_share_at_max`다. 단계 진행률은 `level / levels`이고, 기본 노출이 1 이하일 때 상품 비중은 `진행률 × product_share_at_max`다. 기본 노출이 1보다 크면 기존 기본 상품 비중에서 명시한 최고 상품 비중까지 보간하며, 최고 비중이 기본 노출이 요구하는 상품 비중보다 작으면 오류를 낸다.
- 계좌 곡선은 `effective_exposure = core_weight + leverage_multiple × product_weight`를 명시한다. `SimulationResult`, 새 grid 행, leaderboard 후보 행은 최고 단계의 `effective_exposure_max`를 노출한다. baseline 행은 `product_share_at_max=null`, `effective_exposure_max=1.0`; 배분 결정이 없는 leaderboard 행은 두 필드를 `null`로 두어 결정값처럼 보이지 않게 한다.
- `ladder_levels`는 `BuySignalSpec`을 받을 수 있다. 생략 경로는 현재 B 규칙을 옮기는 동안만 쓰는 얇은 호환 경로이며, 이때도 두 임계값은 `LadderSpec`에 명시해야 한다.
- `compute_signal_features`는 기존 close-T 계산기에서 `drawdown252`, `disp60`, Wilder `rsi14`를 재사용한다. D는 지정 N일 낙폭 변화, E는 마지막 낙폭 구간 진입 뒤의 누적 저점을 인과적으로 계산한다. **E의 저점은 그 시점까지의 최저값(사후 저점 아님) — 헛발 신호가 정직하게 나온다.** 미래 결과 열은 feature/signal 입력에 사용하지 않는다.
- 기존 홀드아웃 시작일, 열람 횟수, 후보/채택 상태, 3단계 고원 문구, `index_basis`, 포워드 기록 형식은 바꾸지 않았다. 기존 schema의 compound grid는 읽을 수 있지만 상품 비중이 없는 과거 행은 새 leaderboard 교차참조에서 `unavailable`로 남아 새 결정값처럼 표시되지 않는다.

## 호출 예

값은 호출자가 명시한다. 아래 숫자는 새 기본값이 아니라 인자 모양을 보이는 예시다.

```python
spec = LadderSpec(
    drawdown_threshold=chosen_drawdown,
    disp60_threshold=chosen_disparity,
    product_share_at_max=chosen_product_share,
    levels=chosen_levels,
    base_exposure=chosen_base_exposure,
)
```

네 CLI 실행기에는 `--drawdown-threshold <결정값>`, `--disp60-threshold <결정값>`, `--product-share-at-max <결정값>`, `--levels <결정값>`, `--base-exposure <결정값>`을 모두 넘겨야 한다. 하나라도 없으면 retained 데이터 로드나 결과 쓰기 전에 `<이름> is undecided under rule ⑥; caller must pass it explicitly` 오류를 낸다. base-exposure sweep은 코드가 1.0을 보충하지 않고 호출자가 넘긴 목록만 순회한다.

## 매수 후보 A–E

| kind | 정의 | 필수 파라미터 |
|---|---|---|
| A | `drawdown252 <= drawdown_threshold` | `drawdown_threshold` |
| B | A이고 `disp60 <= disp60_threshold` | `drawdown_threshold`, `disp60_threshold` |
| C | A이고 `rsi14 <= rsi14_threshold` | `drawdown_threshold`, `rsi14_threshold` |
| D | A이고 `drawdown252.diff(N) >= -tolerance`; 낙폭 확대가 멈추거나 허용 오차 안으로 둔화 | `drawdown_threshold`, `deceleration_lookback_days`, `deceleration_tolerance` |
| E | 마지막 `drawdown252 <= drawdown_threshold` 구간 진입 뒤 누적 저점 대비 `rebound_pct` 이상 반등 | `drawdown_threshold`, `rebound_pct` |

각 kind에 필요한 파라미터가 빠지면 그 파라미터 이름과 규칙 ⑥을 포함한 `ValueError`를 낸다. A–E는 후보 정의일 뿐 채택된 투자 규칙이 아니다. E는 개념적으로 A–D 위에 놓이는 layer-2 진입 시점 옵션이다. 사용자가 A–D의 순위를 정한 뒤 `entry: immediate | rebound_confirmed`로 재구조화할 예정이며, 이번 변경에서는 그 구조 개편을 구현하지 않았다.

## 매수/매도 점수 계약

- `score_buy_events`: 21·63·126·252 retained session의 전방 수익률 평균, 중앙값, 양수 비율을 계산한다.
- `score_sell_events`: 호출자가 넘긴 각 horizon의 전방 연환산 실현변동성과 경로 최대낙폭을 각각 평균·중앙값으로 계산한다. 전방 수익률이나 승률로 매도 신호를 판정하지 않는다.
- 두 함수 모두 `events_total`과 기존 leaderboard의 90일 사건 묶음 helper를 재사용한 `events_independent`를 낸다. 독립성 규칙을 복제하지 않았다.
- leaderboard의 매수 headline은 `return_win_rate`, 과열/매도 headline은 `realized_volatility_max_drawdown`으로 분리했다. 과열 후보의 `disp60` 상단, `rsi14` 상단, `volidx_pct` 하단 조합을 모두 점수에 포함한다.
- `result_card`는 한국어 한 줄 정의, 총/독립 사건 수, 1·3·6·12개월 평균·중앙값·승률 표, 신호일 100 기준 ±252-session 평균 경로를 낸다. 카드 validator는 사건 수나 중앙값 필드가 빠진 카드를 거부한다.
- 규칙 ⑦에 따라 buy card 행은 수익률·승률을 사건 수보다 먼저, sell score 행은 실현변동성·최대낙폭을 사건 수보다 먼저 둔다. 표의 첫 판단 열이 side의 판정 근거와 일치한다.

## 규칙 ⑥ fail-loud 지점

- `src/stock_data/research/compound_ladder.py`: `LadderSpec`의 다섯 미정값을 전부 fail-loud 검증한다.
- `src/stock_data/research/signals.py`: A–E별 필수 임계값/기간/허용오차/반등률 검증.
- `scripts/research/run_compound_backtest.py`: `run`, base-exposure sweep, current-rule 선택, grid builder, CLI에서 다섯 결정값 중 하나라도 누락된 경우.
- `scripts/research/run_core_ammunition.py`: episode 입력, 기본 연구, follow-up, CLI에서 다섯 결정값 중 하나라도 누락된 경우.
- `scripts/research/run_crisis_overlay.py` 및 `src/stock_data/research/crisis_overlay.py`: overlay 생성과 CLI에서 다섯 결정값 중 하나라도 누락된 경우.
- `scripts/research/run_foreign_transfer.py` 및 `src/stock_data/research/foreign_transfer.py`: 원시 임계값, 정규화 임계값, transfer 실행, CLI에서 다섯 결정값 중 하나라도 누락된 경우.
- `rule_candidates.py`는 registry의 모든 indicator에 `threshold`가 있어야 한다는 기존 exact-schema 검증을 유지한다. `forward_test.py`는 그 version-bound 명시 정의만 소비하며 임계값을 보충하지 않는다.
- 과거 compound grid를 읽는 `rule_leaderboard.py`는 `product_share_at_max`와 `effective_exposure_max`가 없는 행을 새 결과로 가장하지 않고 교차참조를 `unavailable`로 처리한다.

이번 보강에서 바꾼 `LadderSpec(...)` 호출부는 다음과 같다.

- `src/stock_data/research/crisis_overlay.py:241` — `build_ladder_overlay`
- `scripts/research/run_compound_backtest.py:537`, `:551`, `:676` — `_grid_for_series`의 signal/grid/current spec
- `scripts/research/run_compound_backtest.py:1199`, `:1247` — `_base_sweep_series`의 signal spec과 caller 목록 반복 spec
- `scripts/research/run_core_ammunition.py:224` — `_episode_inputs`
- `scripts/research/run_foreign_transfer.py:197` — `_variant`

그 밖의 `src/`, `scripts/`, `tests/` 호출부도 전부 감사했다. 정상 생성은 다섯 필드를 명시하고, 새 누락-필드 회귀 테스트만 의도적으로 한 필드씩 제거한 `**kwargs`를 사용한다. `tests/unit/research/test_no_default_ladder_values.py`는 연구 source/script 전체에서 금지 literal을 검사하며 현재 일치 항목은 0개다.

위기 overlay 새 산출물은 `signal_definition`에 kind B, 실제 사용한 다섯 필드, `source: caller`를 남긴다. 이 필드 도입 전 schema-v2 산출물은 재계산하지 않으며 reader 호환 경로에서는 `signal_definition: null`로 취급한다.

## PIT·분할 점검

- feature는 각 행의 T 종가와 그 이전 값만 사용한다. D의 `diff(N)`은 과거 행만, E의 저점 상태는 앞에서 뒤로만 갱신한다.
- `ladder_levels`의 관측 단계는 기존처럼 한 retained session 뒤에 `executable_level`이 된다.
- 전방 return/volatility/MDD는 `scoring.py`에 격리되어 신호 feature namespace로 역류하지 않는다.
- 최종 holdout 경계와 열람 카운터는 변경하지 않았고 이번 작업에서 holdout 또는 전체 grid를 실행하지 않았다.

## 변경 파일

- `src/stock_data/research/compound_ladder.py:77` — `require_levels`, `require_base_exposure`, 다섯 필드 `_UNDECIDED` guard와 export.
- `src/stock_data/research/crisis_overlay.py:223` — 다섯 caller 값 전달, `signal_definition`, legacy-null reader 호환 및 검증.
- `src/stock_data/research/scoring.py:102` — sell의 변동성·MDD 우선 순서와 buy result-card의 수익률·승률 우선 순서.
- `scripts/research/run_compound_backtest.py:474` — grid/current/sweep 전달, caller-owned current 값과 두 새 CLI 인자.
- `scripts/research/run_core_ammunition.py:197` — episode/base/follow-up 전체에 다섯 값 전달, 두 새 CLI 인자.
- `scripts/research/run_crisis_overlay.py:49` — overlay와 core episode 생성에 다섯 값 전달, 두 새 CLI 인자.
- `scripts/research/run_foreign_transfer.py:185` — normalized/raw/KOSPI 변형 전부에 다섯 값 전달, 두 새 CLI 인자.
- `tests/unit/research/test_no_default_ladder_values.py:36` — 필드별 누락 guard, 금지 literal scan, 전체 호출부 AST audit.
- `tests/unit/research/test_crisis_overlay.py:175` — caller signal 정의와 legacy `null` 호환.
- `tests/unit/research/test_scoring.py:50` — side별 첫 판단 열 순서.
- `tests/unit/research/test_compound_ladder.py:542`, `tests/unit/research/test_signals.py:118` — CLI 파싱과 기존 호출부의 다섯 필드 명시.
- `docs/gui/RESEARCH_PARAM_LAB.md:134` — ⑥의 부분-guard 교훈과 규칙 ⑦.
- `docs/research/REBUILD_2026-09-06.md:7` — 본 결과 파일과 재개용 근거.

## 테스트

- guard 단독: `.venv\Scripts\python.exe -m pytest -p no:cacheprovider tests/unit/research/test_no_default_ladder_values.py --basetemp=.tmp/agents/ladder-rule6-01a072a8/pytest-guard-final` → `7 passed`; 금지 literal 일치 `0`, 정상 호출부 누락 `0`.
- 요청 회귀 전체: `.venv\Scripts\python.exe .tmp/agents/ladder-rule6-01a072a8/pytest_runner.py -p no:cacheprovider tests/unit/research tests/unit/web/test_research_page.py --basetemp=.tmp/agents/ladder-rule6-01a072a8/pytest-final2` → `162 passed, 2 warnings in 6.07s`; `src/stock_web/**`는 수정하지 않았다.
- 문법/진입점: research·script·test `compileall`과 네 연구 CLI의 `--help` → `COMPILE_AND_HELP_OK`.
- 네 CLI에 각각 한 필드만 생략한 비산출 probe: `levels` 누락과 `base_exposure` 누락 모두 해당 필드 이름의 규칙 ⑥ 오류 → `ALL_FOUR_MISSING_LEVELS_FAIL_LOUD`, `ALL_FOUR_MISSING_BASE_FAIL_LOUD`.
- Windows에서 pytest의 POSIX mode 임시 디렉터리 ACL 문제를 피하려고, 저장소에 남지 않는 `.tmp/agents/ladder-rule6-01a072a8/pytest_runner.py`가 `os.mkdir`의 mode 인자만 무시하도록 했다. 테스트 내용이나 수집 범위는 바꾸지 않았다.

## 사용자 결정 필요

- 실제 운용/연구별 `drawdown_threshold`, `disp60_threshold`, `product_share_at_max`, `levels`, `base_exposure`: 어떤 실행기에도 코드 기본값이 없다.
- 채택할 매수 후보 kind A–E. 현재 구현은 후보 비교 구조이며 어느 것도 채택으로 표시하지 않는다.
- 채택 후보의 `drawdown_threshold`; B의 `disp60_threshold`; C의 `rsi14_threshold`; D의 `deceleration_lookback_days`와 `deceleration_tolerance`; E의 `rebound_pct`.
- 기존 `rule_candidates.json`의 수치는 기존 후보를 재현할 때만 명시적으로 사용할 수 있다. 이를 사용자 채택값이나 새 기본값으로 승격할지는 별도 결정이 필요하다.

## 남은 일

- 요청 범위의 구현·문서·검증 leftover는 없다.
- 결정값이 없으므로 overlay, grid, sweep, retained 결과, 기존 `RESULTS_*`, hold-out counter는 재생성하거나 변경하지 않았다.
- 작업 시작 전부터 있던 `artifacts/analysis/kospi200_option_wall_recent_250.csv` 수정과 미추적 `uv.lock`은 그대로 보존했고 이번 결과에 포함하지 않았다.
