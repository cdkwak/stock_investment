# Final audit-the-audit decision

## Publication decision

**The evidence collection is now procedurally complete, and `FAIL` is the legal product verdict.** The supplemental protocol closes the prior post-action, interruption, returning-user, lifecycle, round-trip, data-seasoning, dynamic Account-table, and performance gaps. The retained interaction timing remains plausible, the native safety/console/performance gates are evidenced, and screenshot-proven layout collapse plus the High Account keyboard defect independently require `FAIL`.

The revised report also now has a locked persona, standard severity tally, ranked Top 5 with relative rationale, split/merged findings, a three-bucket roadmap, and a genuine `Hold this in your hands` paragraph. The stale first-run Index focus result remains correctly superseded.

### Evidence now sufficient

- `supplemental.json` records 10/10 expected-result post-action assertions with visible/enabled state, text-state digests, latency, and zero new Qt messages.
- The five previously thin scenarios now have concrete protocols and retained images: Equity A→B→A interruption, first/second Research visit, three Account lifecycle states, Dashboard stateful A→B→A, and Day 0/1/7/30 horizons.
- The dynamic Account table completed 0/1/100/1000 rows and restored its original eight rows.
- Performance now uses fixed metric-specific budgets and repeated samples. Startup, direct tab, safe action, and resize median/p95 all pass; the deliberate 700 ms observation wait is separately retained and correctly excluded.
- There are 154 PNGs on disk, 16 structurally complete findings, and all cited finding locations remain concrete and committable.

## Publication corrections resolved

All three final report corrections are resolved:

1. The final Audit artifacts bullet now reports the correct 154 retained images, consistent with measured coverage, `INTERACTION_MANIFEST.md`, and the directory contents.
2. The verdict metadata now explicitly states `Interaction Manifest: complete (60/60 required entry types; 6 per surface)`.
3. The accessibility hard-gate wording now correctly identifies the High Account Tab omission, UX-05, as the gate breach while retaining UX-07 as a separate Medium finding.

`verify_audit.py` now asserts these publication requirements.

## Final classification

| Finding | Class | Final reasoning |
|---|---|---|
| UX-01 | **KEEP** | The Dashboard control collapse is specific, screenshot-proven from 1920 downward, and rooted in fixed non-wrapping toolbar layouts. It is a valid High layout gate failure. |
| UX-02 | **KEEP** | Net Worth visibly sends reload and deletion controls off-screen at 1366/1280. The horizontal-scroll-only recovery path and single-row header location are exact. |
| UX-03 | **KEEP** | Research all-open loses the end of source/status content only at the measured 1280 breakpoint with no vertical recovery. Duplicate screenshots are correctly treated as confirmations of one defect. |
| UX-04 | **KEEP** | The shared provenance formatter finding now consolidates Research, Data Status, Index detail, Account, and Backtest raw-contract language while retaining the distinct missing Research recovery route. |
| UX-05 | **KEEP** | `followup.json` directly proves the Account display-range selector is absent from the real Tab loop despite direct focus succeeding. This is the valid High native accessibility gate breach. |
| UX-06 | **KEEP** | The Account safe-read versus local-mutation hierarchy is now separated from keyboard navigation and has its own visual root and patch. |
| UX-07 | **KEEP** | The unnamed Data Status lifecycle table is machine-proven, specific, and no longer mixed with the shared provenance-copy defect. Medium is appropriate. |
| UX-08 | **KEEP** | The unique Index right-edge price-pill clipping remains after indicator-toolbar duplication was merged into UX-01. Its plot-margin patch is distinct. |
| UX-09 | **KEEP** | The built-in Samsung and SOXX examples visibly lead to failure under the retained local catalog state. This is specific first-use trust damage with a bounded catalog-aware patch. |
| UX-10 | **KEEP** | Backtest's conflicting preserved/no-result state and broken-looking empty plots are a distinct hierarchy defect. Raw-token wording should remain owned by UX-04, but the unique result-state/empty-plot finding survives. |
| UX-11 | **KEEP** | Korean prompts with native English `OK`/`Cancel` are directly visible and share one exact dialog mechanism. This is a valid Low localization issue. |
| UX-12 | **KEEP** | Dashboard long-copy clipping reproduces at the maximum display and maps directly to the fixed three-line card height. It is not the same root as UX-01's responsive toolbar. |
| UX-13 | **KEEP** | Equity and US ETF share one result-feedback height bug, correctly represented as one cross-surface finding rather than one duplicate per page. |
| UX-14 | **KEEP** | The populated Account evidence distinctly shows collapsed legend identity, crowded pie labels, and unreadable date meaning. The chart-specific patch is independent of UX-06. |
| UX-15 | **KEEP** | Research's pointer-empty-state defect is visible across three compact presets and is not solved by the unrelated candidate refresh. The shared `종목 선택` action is concrete. |
| UX-16 | **KEEP** | The before/after Backtest filmstrip shows loss of page identity after the retained interaction sequence, and the suspected scroll-area/focus location plus preservation patch are specific. Keep the reproduction sequence unchanged in future retests so causality remains isolated. |

Final totals: **Drafted 16; KEEP 16; GENERIC 0; DUPLICATE 0; WRONG 0.**

The audit is publication-ready as a procedurally complete **FAIL**. No product, provider, account, scheduler, or Queue change is needed to settle the audit itself.
