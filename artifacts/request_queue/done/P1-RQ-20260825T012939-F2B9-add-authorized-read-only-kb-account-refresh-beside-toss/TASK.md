# Add authorized read-only KB account refresh beside Toss

## Problem
The accepted Account page reads KB and Toss local projections together, but only Toss has authorized manual-click runtime retrieval; KB remains offline-only and its exact SSQM2952 transport/account configuration is unresolved.

## Evidence
B13C accepted the strict domestic-KRW contract while explicitly blocking live execution. 5CBF is the required injected-only crash-safe coordinator and is not yet Done. Current Data Status says exact endpoint URL/method, account-selection mechanism, injected client operation, and desktop environment names are unresolved.

## Scope
allow:
- After dependencies and official route authority, add exact injected KB account client/runtime plumbing and manual-click-only independent GUI refresh with offline tests and current status/runbook truth updates.

deny:
- Endpoint/method/account/env guessing, live call without separate authorization, periodic/startup provider call, raw/full response or identifier persistence, cross-provider fallback/merging, unsupported balances/overseas fields, orders/transfers, scheduler/canonical/history/backtest writes, unrelated files.

## Done When
Only after 5CBF and BFC5 are independently accepted and current Data authority supplies the exact official transport/account configuration, add a one-call injected KB client operation and manual-click-only desktop wiring. One click may start independent Toss and KB cycles off the GUI thread; startup performs local reads/API0, busy clicks coalesce, each provider preserves its own prior valid projection and reports its own state, and KB remains domestic KRW with unsupported fields N/A. No first real call occurs in this task unless a separately explicit bounded operation authorizes it.

## Verify
Injected offline client/runtime/GUI tests cover exact request allowlist, timeout/call ceiling, valid-empty, independent mixed success/failure, prior-valid preservation, click coalescing, startup API0, privacy/log redaction and clean QThread shutdown. Run owning provider/runtime/GUI suites, diff/link checks; no live call.
