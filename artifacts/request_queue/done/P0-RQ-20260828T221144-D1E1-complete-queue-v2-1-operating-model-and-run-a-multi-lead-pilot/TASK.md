# Complete Queue v2.1 operating model and run a multi-Lead pilot

## Problem
Complete and validate the broader Queue v2.1 operating model as a distinct follow-on to the valid F3C6 Queue v2 receipt, using the pre-existing dirty diff rather than rewriting it.

## Evidence
Nine in-scope files have 410 insertions and 43 deletions; Queue Doctor currently passes; the user explicitly transferred durable Manager ownership and requires a real supervised multi-Lead pilot, workflow snapshot/change log, independent review, and scoped commit.

## Scope
allow:
- Review, complete, test, document, and commit only the declared Queue/orchestration operating-model scope and canonical Queue receipts.
- Add the minimum Manager-owned workflow snapshot/change-log record under artifacts/request_queue and link it from current Queue navigation; update Repository Map only if the new record changes durable location/ownership.

deny:
- Do not inspect, stage, alter, restore, or commit artifacts/analysis/kospi200_option_wall_recent_250.csv or perform Queue item A3D5 investigation.
- Do not rewrite valid F3C6 history, broaden the user-owned Goal, manage broker mutations, expose secrets/account identifiers, or absorb Domain Lead Worker/Reviewer duties into MAIN.

## Done When
A Queue/Orchestration Lead reviews and completes the pre-existing v2.1 diff; Manager, Lead, Worker, Reviewer and Waiting/Ready/Active/Review/Done authority are coherent and migration-compatible; a concise current workflow snapshot and append-only material-change log exist and are navigable; the Lead supervises appropriate Worker and fresh independent Reviewer sessions through its own Orca lifecycle; focused and smallest useful regressions plus Queue Doctor and diff checks pass; only declared files and Queue receipts are committed; artifacts/analysis/kospi200_option_wall_recent_250.csv remains uninspected, unstaged, unaltered, and uncommitted.

## Verify
Run the owning request-queue test module in the task-specific temp area, targeted maintenance-bridge regressions identified by the Lead, Queue Doctor, git diff --check, scoped git diff/status inspection excluding the option-wall CSV, fresh independent review, and verify a scoped commit contains no excluded or unrelated file.
