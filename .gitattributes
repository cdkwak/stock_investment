# These canonical replay files are byte-hash-bound and serialized with LF.
artifacts/backtest/phase1_signal_replay/** text eol=lf
artifacts/backtest/kospi200_frozen_manifest.json text eol=lf

# Phase-1 experiment identity hashes source bytes, so checkout EOL is canonical.
src/market_backtest/*.py text eol=lf
src/market_features/*.py text eol=lf
